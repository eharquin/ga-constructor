# Prompt History

A chronological log of all prompts given to Claude in this project.

---

## 2026-05-04

1. **Initial setup** — First commit of the project.
2. **Prompt history system** — Add a system to log all prompts and maintain memory of past work. Asked Claude to read this file on every session launch.
3. **Project recap** — Asked for a full recap of the codebase. Saved to `docs/recap_2026-05-04.md`.
4. **Auto commit/push** — Asked to automatically commit and push after every file edit/write. Configured via PostToolUse hook in `.claude/settings.local.json`.
5. **Motor translation bug** — `T = exp(V, t/2)` + `K = T >>> A` not moving K. Root cause: `PGA.Exp` returns NaN for nilpotent (ideal) bivectors (sin(|L|)/|L| = 0/0). Fixed by manually constructing translator T = 1 + Ls in `motorExp`.
6. **Point K not drawn** — After translator fix, K still not drawn. Root cause: `PGA.Mul` unreliable for sandwich product. Fixed by implementing analytical sandwich product directly in `motorApply` (both translator and rotor cases).
7. **Rotation from point** — Asked to support `M = exp(A, s)` where A is a PGA point to create a rotation motor. Implemented: extracts (px, py) from point, builds motor `cos(s) + sin(s)*(e12 - py·e01 + px·e02)`. `K = M >>> B` then rotates B around A by angle 2s.

## 2026-05-05

8. **Dualization + general multivectors** — Added `!A` (dual of named object), `!(mv_expr)` (dual of inline literal), and bare multivector expressions like `5e02 - 1e01` or `e1 + e2 + 5e0`. Implemented: `parseMVExpr` tokenizer in `parseExpression.js`, `multivector` and `dual` node types in `nodeTypes.js`, extended `lineBaseAndDir` in `pga.js` to handle grade-1 elements (so duals of points render as lines), and unified the Canvas else-branch to auto-detect point vs line for all node types.
12. **General multivector arithmetic expressions** — Added `mvExpr` node type for arbitrary PGA arithmetic: `C = A + B`, `M = (A + B) / 2`, `D = 2*A - B`, `P = A * B` (geometric product), etc. New `evalMVArith.js`: tokenizer, syntax validator, recursive-descent evaluator. Built-in basis blades always available without being deps. Result auto-rendered as point or line. Hardened `lineBaseAndDir` against non-array inputs. Suggestion banner restricted to freePoint/vector/multivector types only.
11. **Scalar creation banner + parametric points** — Three forms now trigger a "Create: x, y, w / + scalars" banner when variable names are undefined: `P=x*e01+y*e02+w*e12`, `P=point(x,y)`, `V=vector(x,y)`. Clicking inserts scalar items before the expression (w defaults to 1 as e12 weight, others to 0). Drag now updates scalar items for all three forms: `updateFreePoint`/`updateVector` call `tryUpdateScalar` when coords are pure identifiers; `updateDepPoint` inverts the PGA2D e01/e02 coefficient mapping (e01→y·w, e02→−x·w). Parametric multivectors with e01/e02 coeffExprs are now hit-testable as `depPoint` drag type. Parser extended: `parseMVExpr` now returns `{ components, deps, coeffExprs }` and supports `var*blade` terms; `multivector` node evaluates them via `evalExpr`.
10. **Ideal-direction as vector** — `V = e01 + e02` (and any pure e01/e02 expression with no e12 or grade-1 terms) now parses as a `vector` node instead of a raw `multivector`. Detection in `parseExpression.js`: if only indices 4 (e01) and 5 (e02) are non-zero, emit `vector` type with `vx = -(e02 coeff)`, `vy = e01 coeff` (consistent with PGA 2D ideal-point convention).
9. **Switch to PGA 2D** — Migrated from PGA(3,0,1) to PGA(2,0,1). Basis shrinks from 16 to 8 elements. Points are now grade-2 (e01,e02,e12 at indices 4,5,6) instead of grade-3. Lines are grade-1 (e0,e1,e2 at 1,2,3) instead of grade-2. Updated: pga.js (Algebra(2,0,1), all index mappings, simplified lineBaseAndDir to grade-1 only, removed makePlane/toEuclidean2D), nodeTypes.js (PGA(8) arrays, new indices in motorExp/motorApply/meet), parseExpression.js (7-blade index for 2D), Canvas.jsx and ExpressionPanel.jsx (removed toEuclidean2D fallback).
13. **Showcase initial expressions** — Replaced the 7 default items with 12 that demonstrate all features: 4 draggable `point()` nodes, 2 `&` join lines, 1 `^` meet intersection, 1 `(A+B)/2` multivector arithmetic midpoint, 1 animatable scalar `t`, 1 `exp(X,t)` rotation motor, and 2 `>>>` motor-apply nodes.
14. **Literal MV point drag + dual with deps** — `P = e01 + e12` (and any literal grade-2 multivector with e12≠0 and no variable coeffs) is now draggable: new `litMVPoint` drag type in `hitTest`; `updateLiteralMVPoint` rewrites the expression with updated e01/e02 (normalised to w=1). `!(mv_expr)` now supports variable coefficients: removed the `deps.length === 0` guard so `P = !(y*e2 + e0)` parses as `multivector` with `dual: true` and renders as a point.
15. **Dual MV point dragging** — `P = !(y*e2 + e0)` is now draggable. New `dualDepPoint` drag type: detects dual multivectors whose pre-dual e2 (→e01→y) or e1 (→e02→x) blade has a variable scalar coefficient. `updateDualDepPoint` applies the same index remapping (e2→e01→y·w, e1→e02→-x·w) so dragging the point updates only the reachable scalar(s) — for `!(y*e2+e0)` only y-axis movement has effect, matching the behaviour of `P = y*e01 + e12`.
18. **Resizable expression panel** — Wrapped `ExpressionPanel` in a `div.panel-wrapper` whose width is driven by React state (default 300px, min 220px, max 700px). A `div.panel-resize` drag handle (5px, `col-resize` cursor) sits between the panel and the canvas; `onMouseDown` records `startX`/`startW` in a ref, global `mousemove`/`mouseup` listeners (attached in `useEffect`) update the width while dragging. `ExpressionPanel.css` changed from `width: 300px` to `width: 100%`; the old `border-right` moved to the handle's `border-right`.
17. **Multivector label below each expression** — Added `formatMV(val)` in `ExpressionPanel.jsx`: formats any PGA value (PGA array or `{vx,vy}` vector) as a human-readable blade sum like `80e01 + 180e02 + e12`, `3e0 - e1 + 2e2`, `0.9553 + 0.2955e12`. Scalars and null values return null (no label). Vectors are shown as their ideal-point representation. Displayed as `.expr-mv` below the type-colored result line — dim monospace, ellipsis-clipped.
16. **Inline geometric expressions everywhere** — `exp()`, `&`, `^`, `>>>` now accept inline geometry for their geometric arguments (not just named IDs). Added `parseInlineGeom` (blade expr → `mv`, `vector(…)`, `point(…)`, or named `ref`) and `splitTopLevelOp` to `parseExpression.js`. Added `resolveInlineGeom` to `nodeTypes.js`. Updated `motorExp`, `motorApply`, `joinLine`, `meetPoint` computes to use `geom`/`geom1`/`geom2` params. Also added ideal-point (pure e01/e02 MV) → translator path in `motorExp` so `exp(e01, 100)` and `exp(vector(1,0), 100)` and `exp(V, 100)` are all equivalent.
20. **SVG rendering migration** — Replaced Canvas 2D (`canvas.getContext('2d')`) with native SVG rendering. `Canvas.jsx` rewritten: imperative draw loop removed, replaced with declarative `SvgGrid`, `SvgPoint`, `SvgLine`, `SvgVector` React components. All coordinate math, hit-testing, and drag logic unchanged. Lines now clipped by SVG `overflow="hidden"`. Text crisp at any zoom/DPI. Created `docs/recap_2026-05-05-svg.md`, updated `CLAUDE.md`.
19. **Session recap + CLAUDE.md update** — Asked to check CLAUDE.md, update the Current Focus section, and create a new recap file in /docs. Created `docs/recap_2026-05-05.md` summarising architecture, node types, drag system, and all changes since 2026-05-04.

## 2026-05-06

21. **Double-click to add point** — Double-clicking on empty canvas space creates a new `freePoint` at that position. Auto-names using unused letters (E, F, G, H, …) then falls back to P1, P2, … Clicking on an existing draggable object is ignored. Implemented: `ADD_ITEM` reducer action + `pickPointName` helper + `addFreePoint` in `useGraph.js`; `handleDoubleClick` in `Canvas.jsx`.
23. **Anonymous expressions (render-only)** — `NAME =` prefix is now optional. Bare expressions like `point(100,100)`, `e1+e2+e0`, `A & B`, `P1 & P2 & P3`, etc. parse and render without assigning a name. Synthetic ID `_<expr>` is used internally. Anonymous nodes are render-only (skipped in hitTest). `parseExpression.js`: made assignment optional, added `id` variable, replaced `id: label` with `id` across all returns. `Canvas.jsx`: `hitTest` skips nodes where `label === null`.
22. **Triangle area from three-point join** — `A = P1 & P2 & P3` now parses as a `triangle` node (3-operand `&` detection in `parseExpression.js`). Computes `{ triangle, p1, p2, p3, area }` from Euclidean positions. Renders as a transparent dashed-border `<polygon>` via new `SvgTriangle` component. Panel shows `area: X.XX`. Color tag: sky blue `#89dceb`.
25. **Parallel lines meet as vector** — `A ^ B` on parallel lines now returns `{ vx, vy, px, py }` (positioned vector) instead of null. `meet()` in `nodeTypes.js`: when det≈0, computes direction (-b/len, a/len) and anchor at the midpoint between the two lines (average of their base points). `toEuclidean` in `pga.js` guarded with `typeof p.length !== 'number'` to reject plain objects. Canvas detects `'px' in val && 'vx' in val` and renders as `SvgVector`. `toIdealDirection` kept as fallback for other ideal-point PGA arrays.
24. **Zoom-adaptive drag precision** — Dragging objects no longer snaps to integer world coordinates. Added `roundToScale(val, scale)` in `Canvas.jsx` (decimal places = round(log10(scale)), so ~1–3px precision at any zoom). Added `fmtNum(val)` in `useGraph.js` (parseFloat toFixed(6) to strip float noise). All `Math.round()` calls in drag paths replaced in both files.

## 2026-05-11

26. **meet via PGA.Wedge + ideal vector from origin** — Replaced manual Cramer's rule meet with `PGA.Wedge`. Ideal-point results rendered as vectors from world origin with raw PGA magnitude (`toIdealVector`, unnormalised).
27. **Value-based type classification + general PGA normalization** — Added `classifyMV()` and `normalizeMVFinit()`/`normalizeMVIdeal()`/`normalizeMV()` to `pga.js`. Panel colors and canvas rendering now driven by geometric kind (not parser type). `norm`/`inorm` toggle buttons replace unit checkbox; normalization propagates to dependents via `evaluate(nodes, normalizeMap)`.
28. **Blade-only mvExpr + implicit multiplication** — Expressions with only literal numbers and basis blades (no variable deps) now parse as `mvExpr`. Implicit `*` inserted between number and `(...)`, so `5(e1+e2+e0)` works.
29. **New showcase** — 26-item triangle centroid construction demonstrating all features: draggable points, animated scalar, join/meet, medians, centroid, rotor, translator, dual, blade expression.
30. **Clear all button + color mismatch fix** — Added "Clear all" button with confirmation. `colorMap` in `useGraph.js` now uses `classifyMV(val).kind → KIND_COLOR`, matching panel vignettes exactly.
31. **n-ary meet chain + z-ordered rendering** — `L1 ^ L2 ^ L3` parses as `meetChain` node folding `PGA.Wedge`. Canvas rendering split into `backLayer` (lines/polygons) and `frontLayer` (points) for guaranteed z-order.
32. **Full operator set in evalMVArith** — Added `!` (dual), `~` (reverse), `^` (wedge), `&` (Vee), `>>>` (sandwich), `|` (inner product via `PGA.LDot`), `§` (commutator `(AB−BA)/2`), `abs()`/`|a|` (absolute value), `.blade` coefficient extraction with permuted blade support (`A.e21 = −A.e12`). `sqrt` of PGA scalar uses `Math.sqrt(val[0])`.
33. **Triangle = plain scalar + list notation** — `A & B & C` now returns `2×signedArea` as a plain JS number usable in any expression. New `[P1, P2, P3, …]` syntax creates a `list` node (non-MV) rendered as a dashed `SvgPolygon`.
34. **Label templates** — `{varname}` in label text is substituted with the current scalar value of that variable at render time. `labelMap` moved after `values` and resolves templates live.
35. **Expression Reference updated** — Help modal covers all operators, object types with color legend, blade extraction, list notation, normalization buttons.
36. **Session recap + doc update** — Created `docs/recap_2026-05-11.md`, updated `CLAUDE.md`.

## 2026-05-15

37. **Launch the project and give the URL** — Started Vite dev server (`npm run dev -- --host`) and reported the URL.
38. **Movable/lock toggle per item** — Added `movable: true` field on items, `SET_MOVABLE` reducer action, `movableMap` memo, `setItemMovable` API. `Canvas.jsx hitTest` skips the node entirely when `movableMap[id] === false`. UI: small 🔓/🔒 toggle button (`.lock-toggle`) next to the visibility checkbox, shown only for drag-eligible items (`freePoint`, `vector`, multivector with `depPoint`/`dualDepPoint`/`litMVPoint` form). Locked = static, unlocked = draggable (default).
39. **Vector defined from `V = e01` movable in any direction** — Loosened the zero-literal drag constraint in `updateVector`. Previous logic forced `cvx = 0` whenever `xExpr` was a literal "0", blocking diagonal drag even for fully-literal forms like `V = e01`. New rule: constrain to zero only when the *other* axis is a scalar variable identifier (preserves the `V = x*e01` case where the y-axis variable is the only DOF, but allows free drag for `V = e01` or `V = 5e01 + 4e02`).
40. **Dark theme** — Added a CSS-variable theme system. `:root` defines light defaults; `[data-theme="dark"]` overrides. ~30 named variables for backgrounds, text, borders, accents, grid colors, point strokes. Header button writes `theme` to `localStorage` and sets `document.documentElement.dataset.theme`. All hardcoded hex/rgba in `App.css` and `ExpressionPanel.css` replaced with `var(--…)`. Canvas SVG attributes (background `<rect>`, grid lines, axis lines, label text, point strokes, label rendering) use `style={{ fill: 'var(--…)' }}` / `style={{ stroke: 'var(--…)' }}`.
41. **Fade effect on theme switch** — `toggleTheme` adds a `theme-fade` class to `<html>` for 350ms, then removes it. CSS rule applies a 300ms ease transition to `background-color`, `color`, `border-color`, `fill`, `stroke`, `box-shadow` on all elements while the class is present (with `!important` to override per-element transitions). Hover effects keep their original timings outside the swap window.
42. **Trajectory drawing — initial (pre-sampled) version** — Added `trace: false` per item, `SET_TRACE` reducer, `setItemTrace` API, ∿ toggle button (`.trace-toggle`) for trace-eligible items (finite point / ideal vector). Initial implementation: a `trajectoriesMap` memo gathered each traced node's animatable scalar deps, swept each across its `anim` range (`TRACE_SAMPLES = 96` samples), called `evaluate()` with the scalar overridden, and emitted one polyline per varying scalar. Rendered as a dashed polyline behind the object via new `SvgTrajectory` component.
43. **Implement the user's example as the base showcase** — Replaced the 26-item triangle centroid showcase with the 8-item motor composition example: `P = 5.2*e01 + 5.2*e02 + e12`, `V = vector(-6.48, 0.92)`, animatable scalars `t` (0→1) and `a` (0→π), translator `T = exp(V,t)`, rotor `R = exp(e12,a)`, composed motor `M = R*T`, transformed point `Q = M >>> P` (trace on by default). Renamed the user's `R = M>>>P` to `Q` to avoid collision with the rotor `R`. `nextId` updated from 27 to 8.
44. **Trajectory replaced with dynamic trail accumulation during animation** — Replaced the pre-sampled `trajectoriesMap` with a runtime trail. New `trails` state (`{ [nodeId]: [{x,y}, …] }`), useEffect watches `values + playingIds + items` and appends the current position of each traced item whenever values change while at least one scalar is playing. `setItemTrace(id, true)` clears the existing trail so each new trace session starts fresh. `trajectoriesMap` re-shapes the trail into the same `[{scalarId, points}]` shape Canvas consumes, so `SvgTrajectory` is unchanged. Cap: `TRAIL_MAX_POINTS = 5000` (FIFO when full). Removed `TRACE_SAMPLES` / `DEFAULT_TRACE_ANIM` constants. Had to relocate the new block *after* `values = useMemo(...)` is declared (initial placement triggered "cannot access lexical declaration before initialization").
45. **Session recap + doc update** — Created `docs/recap_2026-05-15.md`, updated `CLAUDE.md`, appended this section to `PROMPT_HISTORY.md`.
46. **Launch script** — Added `launch.sh`: runs `npm install` if `node_modules/` missing, starts `npm run dev` in the background, tails its log until a URL appears, prints `➜ Local: <url>`, then `wait`s on the server. Cleans up on Ctrl-C.
47. **`T >>> L` for lines** — `motorApply.compute` in `src/graph/nodeTypes.js` was hardcoded for finite points (returned `null` when the input had no e12 weight). Added a fallback: when the input lacks e12 weight (line, ideal point, …), evaluate the generic sandwich `PGA.Mul(PGA.Mul(T, G), reverseOp(T))`. Points keep the optimized 2D path.

## 2026-05-16

48. **`exp(V, t)` returns literal `1 + t·V`** — Collapsed `motorExp.compute` to two branches: (a) grade-2 with e12 ≠ 0 → proper trig motor `cos(s) + sin(s)·V` (rotation around a point); (b) everything else → nilpotent path returning `1 + s·V` exactly (V² = 0 in PGA(2,0,1) for pure-ideal bivectors, so the Taylor series terminates). Vectors `{vx, vy}` are promoted via `idealPoint(vx, vy)` before the branch, so the sign-flip (`-vx·s, -vy·s`) and the implicit `2×` factor are both gone. Geometric consequence: `T = exp(V, t)` translates points *perpendicular* to V by `2t·|V|` rather than *along* V — clean algebra, different geometry. Showcase still works; the trajectory just goes the other direction.
49. **`V * e12` returns the orthogonal vector** — `parseMVExpr` matches `var * blade` and stores it as a *scalar* coefficient in `coeffExprs`, so `V * e12` with V a vector got `mv[6] = evalExpr('V', …) = NaN`. Patched `multivector.compute` to detect when a coefficient is a single var (optionally negated) that resolves to a non-scalar dep, and accumulate the true algebraic product `dep × blade` instead. Now `V*e12` returns `vx·e01 + vy·e02` (the perpendicular ideal point), and `x*e01 + 2*e02` with x a scalar still works through the original path.
50. **`exp` is now single-argument** — Removed the `exp(G, s)` form. Parser at `parseExpression.js:271` now matches `exp(<mv_expr>)` only (rejects any expression that splits on a top-level comma), delegating the inner argument to `extractMVDeps`/`evalMVArith`. `motorExp.compute` evaluates the argument to an MV V and exponentiates it directly: nilpotent V (V² = 0, i.e. V[6] = 0) → `1 + V`; otherwise `c = V[6]`, return `cos(c) + (sin(c)/c)·V` (handles rotors and finite-point motors uniformly via the closed-form Taylor sum, since V² = −c² for any bivector with no e0 component in PGA(2,0,1)). Scalar input falls back to `Math.exp`. Showcase updated: `T = exp(t*V)`, `R = exp(a*e12)`. Help-modal Motors row rewritten to reflect the new syntax.
51. **Git workflow set up** — GitHub Flow with PR-based feature branches. Auto-commit hook now does `git push -u origin HEAD` so new branches publish on their first commit. Added `docs/git_workflow.md` covering quick-reference commands, branch prefixes, hook behavior + gotchas, PR rules, and recovery paths if work starts on the wrong branch.
52. **Feature audit pass (branch `fix/feature-audit`)** — Systematic sweep across constructors, operators, motors, normalization, interactivity, animation, theming, and showcase. Found three bugs, all fixed:
    - **`.blade` accessor in scalar coord exprs**: `point(P.e01, ...)` failed because `evalCoords` → `evalExpr` (scalar-only) can't resolve `.blade`. Added `evalScalar` in `evalExpr.js` that routes through `evalMVArith` when the expression contains `.<letter>`, else falls back to `evalExpr` (preserves PI/E etc.). Wired into freePoint/vector/freeLine coord eval and `resolveInlineGeom`/multivector coeff fallback.
    - **Blade names appearing as graph deps**: `extractVarNames` picked up `e01`, `e02`, ... as identifiers, polluting freePoint/vector/freeLine deps. The graph engine then short-circuits because `values["e01"]` is undefined. Filtered blade names via the now-exported `parseBladeName` from `evalMVArith.js`.
    - **`!e01` / `~e12`** parsed as "dual/reverse of named node e01/e12" since the `!ID`/`~ID` regex didn't exclude blade names. Skipped those forms when `parseBladeName(id)` returns truthy, letting them fall through to `mvExpr` where the unary ops handle blades correctly. Also exported `parseBladeName` from `evalMVArith.js` for reuse.
    
    Known limitations left as-is: (a) ~~`exp(V)` with V having both e0 and e12 components uses the cos+(sin/c)·V form, exact only when V² is a pure scalar~~ (fixed in #54 by delegating to ganja's `V.Exp()`); (b) `M >>> [P1, P2, ...]` doesn't transform polygons as a unit; (c) operator precedence is flat (single parseTerm level) — `A * B ^ C` is `(A*B) ^ C` not the GA-conventional `A * (B ^ C)`.

## 2026-05-22

72. **Shareable URL** — Encode current graph (items + algebra) into the URL hash so a graph is one copy-paste away. No server needed. New `src/urlHash.js`: `encodeGraph` (JSON → base64url hash) and `decodeGraph`/`readHashGraph` (reverse). `AlgebraContext` and `useGraph` initializers check the hash first, so visiting a shared URL seeds the correct algebra and items before any localStorage restore. `AppShell` clears the hash after mount via `history.replaceState` so the URL stays clean for subsequent edits. New `ShareButton` in the header (`🔗`) encodes the current graph, writes the full URL to the clipboard, and shows a `✓` confirmation for 2 s (falls back to `window.prompt` if clipboard API is denied).

53. **Removed trail/trajectory feature** — Stripped the `∿` trace toggle, `trace` per-item flag, `SET_TRACE` reducer case, `setItemTrace` API, `trails` state, trail-accumulation useEffect, `trajectoriesMap` memo, `SvgTrajectory` component (Canvas.jsx), trace-toggle wiring in ExpressionPanel.jsx, `.trace-toggle*` CSS rules, `TRAIL_MAX_POINTS` constant, and `{ trace: true }` on the showcase Q item. Also updated CLAUDE.md to drop the trace/trajectory sections.

54. **Delegated more operations to ganja** — Replaced hand-rolled GA primitives with ganja's built-ins so we share a single sign convention and shed dead/buggy code (full notes in `docs/recap_2026-05-16.md`):
    - `dualOp` → `PGA.Dual` (right-complement convention; fixes the e1↔e02 sign that diverged from ganja).
    - `reverseOp` → `PGA.Reverse`.
    - `motorApply.compute` → `PGA.sw(T, A)`. Drops a ~25-line "optimized 2D point sandwich" path that was *incorrect* for composed motors (it treated T[4]/T[5] as the rotation center, valid only for pure rotations — composed translation+rotation motors gave wrong results).
    - `motorExp.compute` → `V.Exp()` (instance method). Drops the closed-form `cos(c)+(sin(c)/c)·V` branch and also **fixes the previously-noted limitation** for V with both e0 and e12 components (V² no longer needs to be a pure scalar).
    - `evalMVArith` `>>>` → `PGA.sw` (was `PGA.Mul(PGA.Mul(M,A), reverseOp(M))`).
    - `evalMVArith` `sqrt(motor)` → `M.Log()` → halve → `.Exp()`. The old code called `PGA.Sqrt` which **doesn't exist on the static class**; the call was silently swallowed by `evaluate.js`'s try/catch, so `sqrt(M)` had been broken since it was written.
    - `point2D`, `idealPoint`, `line2D` → use `PGA.Bivector`/`PGA.Vector` typed constructors.
    - `finitNormSq` removed; `normalizeMVFinit`, `normalizeMVIdeal`, `normalizeMV` now call `PGA.Length` (and `PGA.Length(PGA.Dual(val))` for the ideal-norm path).

## 2026-05-17

55. **CLAUDE.md / docs / PROMPT_HISTORY refresh** — Updated `CLAUDE.md` Current Focus to call out the ganja delegation, single-argument `exp`, weight-driven render thickness, ideal-line ellipse + ideal-point markers, and the git workflow doc. Created `docs/recap_2026-05-16.md` covering prompts 48–54 in detail (algebra cleanup, feature audit, trail removal, ganja delegation, visual weight, ideal-point rendering). Marked the `exp(V) with mixed e0/e12` limitation in #52 as resolved by #54.

56. **Launch the project** — Started Vite dev server, reported the URL.

57. **Saved-graph functionality** — Filesystem-backed dev store via a Vite plugin (`/api/graphs` CRUD against `saved_graphs/`). Header `💾` Save / `📂` Open buttons + modal list with Load + Delete. Items only — viewport/theme excluded. Names slugified to `[A-Za-z0-9_-]`. Explicit-save model (no autosave). Added `LOAD_ITEMS` reducer + `loadItems(items)` API in `useGraph.js`. New CSS for the icon buttons, modal frame, and saved-graphs list. `/saved_graphs/` added to `.gitignore`.

58. **VGA(2,0,0) as a second algebra** — Cut `feat/vga-algebra` off `fix/feature-audit`. Refactored every PGA assumption out of the parser/evaluator/nodeTypes into per-algebra adapter modules under `src/algebras/<id>/index.js`. Graph modules (`parseExpression`, `evalMVArith`, `nodeTypes`, `evaluate`) became factory functions; each adapter binds them against its own spec (`Algebra` instance, `bladeIndex`, `parseBladeName`, `classifyMV`, `getRenderPlan`, `supportedNodeTypes`, `INITIAL_ITEMS`, `KIND_COLOR`). Added `src/AlgebraContext.jsx`; `GraphProvider` reads the active algebra from it and threads it through `useGraph(algebra)`. Header dropdown wired (confirm-then-reset). Saved graphs gain an `algebra` tag — loading a cross-algebra save prompts the user to auto-switch first. **New VGA renderers in `Canvas.jsx`:** `SvgBivector` (oriented loop at origin, radius ∝ √\|b\|) and `SvgRotor` (arc spanning 2·atan2(b, a) with angle label). VGA showcase: `V = 3*e1 + 2*e2`, `W = vector(-1, 2.5)`, `S = V|W`, `B = V^W`, `R = exp((a/2)*e12)`, `V_rot = R >>> V`. Full notes in `docs/recap_2026-05-17.md`.

59. **Remove magnitude-driven thickness on VGA bivector** — `SvgBivector` was scaling the loop radius by `√|b|` and stroke width by `weight = objectWeight(val)`. Both removed. Loop now renders at fixed radius (22) and stroke (2); only the sign of `b` drives the curved-arrow direction. Magnitude is communicated by the panel label, not the canvas geometry.

60. **URL request** — Reported `http://localhost:5173/` (Vite dev server).

61. **Bivector V^W as oriented parallelogram** — When `B = <id> ^ <id>` and both deps resolve to vectors, `Canvas.jsx` now renders an `SvgWedgeParallelogram` (vertices at `0`, `V`, `V+W`, `W`) with a centroid arc indicating sign. Area scales directly with `|V^W|`. Detection is inline in the bivector render branch; literal `b*e12` or compound wedge expressions still fall back to `SvgBivector`.

62. **Every vector tail draggable + ref to another vector's tip** — Derived vectors (`U = V + W` → mvExpr classified as `vector`) had no entry in `vectorPositions`, so dragging didn't work. Extended the memo: any value classifying as `vector`/`idealPoint`/`{vx,vy}` is eligible. Resolved `drawPos.ref` against another node's tip in a two-pass loop (so ref chains converge). Hit-test, `findVectorItem`, and `findNearbySnapTarget` extended to match these. Tip drag stays restricted to explicit `vector` nodes — derived vectors expose only a tail anchor.

63. **Reorder bug in expression panel** — `REORDER` reducer case was dropped during the algebra-refactor rewrite of `useGraph.js`; the action dispatched but fell through to `default`. Restored.

64. **Position anchor for bivectors + tail-or-tip refs** — Both `SvgBivector` and `SvgWedgeParallelogram` now take `(px, py)` and render the anchor (loop centre or origin corner) at that world position with a draggable grab-handle. `vectorPositions` includes bivector-valued nodes; `drawPos.ref` extended with `anchor: 'tail' | 'tip'` (default `'tip'` for back-compat). `findNearbySnapTarget` returns `{id, anchor}` — picks tail vs. tip by distance, ties go to tip. Panel position sub-row accepts `Name.tail`, `Name.tip`, or `(x, y)`. The system is symmetric across vectors and bivectors in both algebras.

65. **Dual + reverse undefined for vectors** — `nodeTypes.js` `dual.compute` and `reverse.compute` bailed out when `val` lacked `.length` ≥ arraySize, so `!V` for a `{vx, vy}` vector always returned null. Fix: a beefed-up `toMV` helper that promotes scalar numbers and `{vx, vy}` to algebra MVs before applying `dualOp` / `reverseOp`. `!V` now works in both algebras (PGA: vector → perpendicular line; VGA: vector → 90° rotated vector, the standard `V*e12` polar dual). Ganja's `Algebra(...).Dual` already gives the right convention for each — no override needed.

66. **Options menu** — Added `src/SettingsContext.jsx` (settings + localStorage persistence at `ga-settings`) and a `⚙` popover in the header next to the theme toggle. Settings: `weightThickness` (default **off** per user request), `showMvExpression` (default **on** per user request), `showGrid`, `snapOnDrag`, `alwaysShowAnchors`, `decimals` (2/4/6 dropdown). Canvas consumes weight/grid/snap/anchor-visibility; `ExpressionPanel` consumes the MV-line toggle and threads `decimals` through `getDisplayValue` and `formatMV`.

67. **Git status recap** — Reported branch `feat/vga-algebra`, 27 commits ahead of `main`, clean working tree, no PR opened. Summarised file groups: saved graphs, algebra adapters, graph factories, VGA renderers, anchor system, dual bugfix, options menu, docs.

68. **Backfill prompts 59–67** — Appended this section to `PROMPT_HISTORY.md` to cover the work that happened on `feat/vga-algebra` after the recap doc was written. Branch is now fully documented before opening the PR.

## 2026-05-20

69. **Plan + branching strategy** — Asked for next steps. Agreed on three independent PRs in this order: undo/redo, operator precedence, list values. Each off `main`, not stacked.

70. **Undo / redo (branch `feat/undo-redo`, PR #2)** — Wrapped `useGraph`'s reducer state in `{ items, past, future, lastChange }`. Refactored existing pure transforms into `itemsReducer`; outer `reducer` handles `UNDO`/`REDO`. High-frequency `SET_TEXT`/`SET_DRAW_POS` writes to the same id within 400 ms coalesce into one history entry. `loadItems` got `{ fromAlgebraSwitch }` to reset history on algebra change. Stack capped at 100. `↶`/`↷` header buttons + `Ctrl/Cmd+Z` / `Ctrl/Cmd+Shift+Z`.

71. **Operator precedence + norm extraction (branch `feat/operator-precedence`, PR #3)** — Replaced flat `parseTerm` in `evalMVArith.js` with a 4-level precedence ladder: `^ & | §` (tightest) → `* /` → `>>>` → `+ -` (loosest). Mirrored in `validate()`. `A * B ^ C` now parses as `A * (B^C)`; `M * N >>> A ^ B` as `(M*N) >>> (A^B)`. Also added norm extraction: `|expr|` uses smart norm (auto-detects finite vs ideal via `classifyMV`; plain `Math.abs` for scalars); `(expr).norm` forces finite norm (`Algebra.Length`); `(expr).inorm` forces ideal norm (`Algebra.Length(Dual(…))`). Fixed `|…|`/`|` ambiguity with `absDepth` counter. Added 5 example saved graphs.

## 2026-05-28

72. **Scalar slider below scalar expressions** — Added an `<input type="range">` row below each scalar expression that scrubs the value within its `[min, max]` animation interval. Dimmed when animMode is `'infinite'`.

73. **Zero shown below scalar (3 rounds)** — Fixed `expr-result` showing `0` for zero-value scalars. Root cause chain: `'0'` truthy → `expr-result` rendered; then `formatMV` returning `'0'` → `expr-mv` rendered; then `hasPosition` returning `0` (falsy `||` chain) → React rendering `{0}` as text. Fixed with `!isScalar` guards on first two, `!!` coercion on last.

74. **Non-draggable hollow ring** — `Q = M >>> P` (motorApply result) should not show the transparent hollow ring. Added `draggable` prop to `SvgPoint` and gated the ring on it; `isDragEligible` computed from node type.

75. **Point ring size + vector tail appearance** — Reduced hollow ring radius from 14 to 10. Gave vector tails the same ring + animated-dot appearance as draggable points.

76. **Independent tail/tip hover** — Split `hoveredId` into `hoveredId` + `hoveredDragType`. `tailHovered`/`tipHovered` derived from `hoveredDragType !== 'vectorTip'`. Hovering tip no longer animates tail, and vice versa.

77. **Tooltip rename** — Changed color swatch `title` from `'Change color'` to `'Object menu'`.

78. **NumStepper buttons** — Added `−`/`+` increment buttons to opacity, size, orientation, and font-size fields in `AppearancePanel`. New `NumStepper` component wrapping `NumInput`.

79. **Variable references in appearance fields** — Opacity, size, label angle, and label font-size inputs now accept scalar variable names (e.g., `t`). Typing a variable name stores it as a string in `item.opacity`/`item.scale`/`labelOpts.orientation`/`labelOpts.fontSize`. Canvas resolves via `resolveField(raw, values, fallback)`. AppearancePanel shows variable name in italic with resolved value as tooltip; `+/-` buttons disabled when a variable is active.

80. **Scalar interval-editor visibility + slider bounds** — Interval sub-row (`(min, max, step)` input) now appears only while the scalar's expression input (or the interval input itself) has focus; tracked via `editingId` state with a 0-tick blur timer so focus can move between expression ↔ interval inputs without flicker. Slider row now shows min on the left and max on the right of the track (`scalar-slider-bound` spans).

81. **User-defined functions** — New top-level form `name(p1, p2, ...) = body` parses as `funcDef`. `evalMVArith` tokenizer accepts `,`, validator and `parseFactor` recognise multi-arg id(arg, …) calls, dispatching to builtins (`sqrt`, `sin`, …) or to user-function values `{ kind: 'function', paramNames, body, capturedEnv }` looked up in the eval env. Recursion allowed with `MAX_USER_CALL_DEPTH=64`. Body free vars become `funcDef`'s `deps` so global references propagate reactively (e.g. `shift(P) = P + Q` re-evaluates when Q changes); the function's own name is filtered out of deps so direct recursion doesn't create a cycle. Names colliding with builtins (`BUILTIN_FN_NAMES` + `point`/`line`/`vector`/`color`/`triangle`/`exp`) reject at parse — row turns invalid. `ExpressionPanel` shows a `ƒ` badge (`.func-badge`) in the swatch slot and a `Function (A, B)` descriptor below the input; no value display, no canvas render (classifyMV/getRenderPlan already return null for function shapes). `funcDef` added to `SUPPORTED_NODE_TYPES` in both PGA and VGA specs.

82. **List menu — Points/Segments/Area, drop Elements** — `AppearancePanel`'s "List" section now has three toggles: Points (new), Segments, Area (renamed from Fill area). Per-element show/hide ("Elements" sub-section) removed. `hiddenElements` field + `TOGGLE_LIST_ELEMENT` action + `toggleListElement` API gone; new `showPoints` field + `SET_LIST_SHOW_POINTS` + `setListShowPoints`. `Canvas.jsx` list-render branch gates finitePoint elements on `showPoints` (default true); outline/fill defaults are Segments=true, Area=true so `[A, B, C]` renders three points + AB/BC segments + filled triangle out of the box.

83. **Segments are pure strokes, default zoom −1…1** — `SvgPolygon` (used only by the Segments toggle) now renders an open `<polyline fill="none">` instead of a closed filled `<polygon>` — Segments draws AB, BC only; the closing CA edge and the translucent fill it carried are gone. Area still renders its own closed filled polygon, so the two toggles are now independent. Bumped `INITIAL_VP.scale` from 30 → 300 so a default 800×600 canvas shows roughly y ∈ [−1, 1] and x ∈ [−1.33, 1.33] out of the box.

84. **Folder group system** — Expressions can now be grouped into single-level collapsible folders. New fields on every item: `parentId`, `kind ('folder'|null)`, `folderName`, `collapsed`. New reducer actions `ADD_FOLDER`, `SET_FOLDER_NAME`, `SET_FOLDER_COLLAPSED`; `DELETE` orphans a folder's children to top level when the folder is removed; `REORDER` gains a `'inside'` position that reparents the dragged item onto the target folder and inserts it at the end of that folder's children block, while `'before'`/`'after'` inherit the target's `parentId` (folders themselves stay rooted). `SET_FOLDER_NAME` is coalesce-eligible so rapid typing collapses into one history entry. New API: `addFolder()`, `setFolderName(id, name)`, `setFolderCollapsed(id, collapsed)`. `ExpressionPanel` got a 📁 header button, a per-row `FolderRow` branch (drag handle, ▸/▾ toggle, folder icon, name input, delete), child rows get `.expr-entry--child` indent (20px), and the drag-zone calculation switches to thirds (before / inside / after) when hovering a folder — suppressed back to halves while dragging a folder so folders can't nest. New CSS: `.expr-entry.drop-inside` (full-row outline), `.expr-entry--folder` background, `.folder-row` layout, `.folder-collapse` / `.folder-icon` / `.folder-name-input` controls. Graph evaluator unchanged — folders have `text: ''` so `parseExpression()` returns null and they're silently filtered out of `nodes` / `orderedNodeIds` / `evaluate`. Items inside a collapsed folder still feed the graph and render on the canvas; only their panel rows are hidden.

85. **`pi` constant** — Lowercase `pi` (= `Math.PI`) is now usable in both scalar and MV expressions. New `SCALAR_CONSTS = { pi: Math.PI }` table in `evalMVArith.js`, merged into `fullEnv` alongside `COLOR_CONSTS` and excluded from `extractMVDeps`'s dep-collection scan. `evalExpr.js`'s `MATH` map gains `pi` so it's also filtered out of `MATH_NAMES` (and therefore `extractVarNames`). Examples: `R = exp(pi*e12)`, `t = pi/2`. Existing uppercase `PI` still works in scalar contexts; the MV side now resolves both via the merged env.

86. **Plain numbers classify as `scalar`** — `r = 2*pi` parses as an `mvExpr` (the scalar form only accepts a single literal number) and evaluates to `6.283…`, but the panel was rendering it with a swatch + norm/inorm buttons because `classifyMV(number)` returned `null` (falling back to grey "unknown"). Updated `classifyMV` in PGA and VGA to return `{ kind: 'scalar' }` for any plain JS number; `r010` (complex-plane algebra) keeps its `finitePoint` semantics. Tightened the panel's `canUnitize` guard with `cls_?.kind !== 'scalar'`, so derived scalar values (`r = 2*pi`, `a = sin(t)`, …) no longer show meaningless norm buttons and pick up the green scalar color via `KIND_COLOR.scalar`.

87. **Canvas recenter button** — Floating top-left button in the Canvas (inline SVG crosshair icon) resets the viewport offset so the world origin sits at the canvas centre. Zoom is preserved — only `offsetX`/`offsetY` are reset to `size.w/2`, `size.h/2`. Positioned at `top: 8px, left: 42px` so it sits to the right of the panel-show button (which only appears when the expression panel is hidden), styled to match (`.canvas-recenter-btn` mirrors `.panel-show-btn`).

88. **Algebra info menu + per-algebra docs** — Header gets an `ℹ` button next to the algebra dropdown that opens `AlgebraInfoModal`. The modal pulls a new `info` field from each spec (`fullName`, `signature`, `description`, `geometry[]`, `subalgebras[]`, `notes[]`) and renders sections: signature header, basis & metric row, Cayley table, object-kinds color palette (driven by `KIND_COLOR`), geometric interpretation, sub-algebras, notes. The basis squares and Cayley table are **computed at runtime** in `src/algebras/cayley.js` from `Algebra.Mul()` on each pair of basis blades — works for any future algebra without hand-authoring. The Expression Reference modal lost its `PGA(2,0,1) basis — 8 blades` and `Object types` sections (both algebra-specific); replaced with a short note pointing at the ℹ menu. Companion markdown docs for each algebra (`docs/algebras/pga201.md`, `vga200.md`, `r010.md`) mirror the modal content for offline reading. New CSS `.algebra-info-grid` + `.algebra-info-cayley` in `App.css` reuses the existing `.help-overlay` / `.help-modal` / `.help-section` scaffold.

89. **CGA(3,1) — 2D Conformal Geometric Algebra adapter** (`feat/cga-2d-adapter` branch) — Fourth algebra: 4 generators (e1²=e2²=e3²=+1, e4²=−1), 16 blades. Conformal points embed Euclidean (x,y) as `P = e0 + x·e1 + y·e2 + ½(x²+y²)·einf` where the **null basis** `e0 = (e4−e3)/2` and `einf = e4+e3` are NOT basis blades — they're injected as named MV constants. New spec field `algebra.mvConsts` consumed by `evalMVArith.js`: merged into `fullEnv` so identifiers resolve to MV arrays, and excluded from `extractMVDeps` dep scan (like `pi`/`SCALAR_CONSTS`). OPNS object encoding via wedge: grade-2 = point pair, grade-3 = line (contains `einf`) or circle (doesn't). Classifier distinguishes line vs. circle by the e0-coefficient of the IPNS dual. New render kinds `circle` + `pointPair` with new `SvgCircle` and `SvgPointPair` components in `Canvas.jsx`. Circle centre/radius extracted from the IPNS dual sphere vector `w·e0 + p_c + ½(p_c²−r²)·einf` after normalizing by `w`. Point-pair extraction uses the standard formula `P± = m⁻¹·(pp ± √(pp²))` with `m = pp·einf`. Showcase: `P1=(1,0)`, `P2=(-1,0.5)`, `P3=(0,1)`, `C=P1^P2^P3` — drag P3 to be collinear with P1, P2 and the circle smoothly becomes a line (CGA's defining unification). Algebra Info modal picks up the 16-blade Cayley table automatically via the runtime helper. Companion doc at `docs/algebras/cga310.md`.

---

## 2026-05-31

90. **CGA round point `point(x, y, r)`** — Extended the CGA conformal point embedding to support a radius parameter: `P = e0 + x·e1 + y·e2 + ½(x²+y²+r²)·einf`. When r=0, produces the standard null point (unchanged). When r≠0, produces a round point (P·P = −r²). Classification uses a new `extractRoundPoint` helper (analytical P·P formula, avoiding Float32 drift). New `roundPoint` kind in `classifyMV`, `getRenderPlan`, `KIND_COLOR`. Rendered as a center dot + outer solid circle (rSq>0) or dashed circle (rSq<0, imaginary). ExpressionPanel shows "Round point (x, y) r=…" descriptor. Drag preserves r (zExpr extracted from node params and reinjected into text). Files changed: `src/algebras/cga310/index.js`, `src/graph/parseExpression.js` (try `parse3Call` first for `point`), `src/graph/nodeTypes.js` (freePoint.compute passes optional r), `src/useGraph.js` (updateFreePoint preserves zExpr), `src/Canvas.jsx` (roundPoint render case), `src/ExpressionPanel.jsx` (descriptor + DRAWABLE_KINDS).

91. **CGA flat point — draggable `flatPoint(x, y)` constructor** — Added `freeFlatPoint` node type with `flatPoint(x, y)` expression syntax (mirrors `freePoint`/`point()`). New `flatPoint2D(x, y)` in `cga310/index.js` constructs `x*(e13+e14) + y*(e23+e24) - e34`. `flatPoint` added to `BUILTIN_CONSTRUCTOR_NAMES` in `parseExpression.js`. `freeFlatPoint` node type in `nodeTypes.js`. Hit test in `Canvas.jsx` uses `getRenderPlan` to find screen position. Drag dispatches `updateFreeFlatPoint` in `useGraph.js` which regenerates `flatPoint(newX, newY)` text. Both `freeFlatPoint` (drag-created) and `mvExpr` flat points (computed via `P ^ einf` or `x*e1inf + y*e2inf + w*e0inf`) render as blue diamonds.

## 2026-06-01

92. **Point-pair dashed segment visibility** — "in CGA when intersecting a circle and a line gives a point pair. But in the renderer the two points are not linked via a dashed line just like a constructed point pair P^Q with P,Q two round points. for each point pair draw a dashed segment between the two points." Investigation: `SvgPointPair` in `Canvas.jsx` already draws a dashed connecting line, and `C&L` (Vee intersection) classifies as `pointPair` and renders via the same path as `P^Q`. The reported "missing" line was the **tangent degeneracy** in `saved_graphs/cga_circle_line_intersection.json` (default `Fx=1`: line x=1 is tangent to the unit circle at (1,0), so both points coincide → zero-length, invisible segment; sliding Fx<1 separates them). Made the dashed segment clearly visible: `strokeOpacity` 0.4→0.6, `strokeWidth` now scales with `weight` (`1.5*weight`), `strokeLinecap="round"`, dash `"4 3"`→`"5 4"`.

93. **CGA `E >>> G` misclassified as Reflector** — "for E and G two points, E>>>G should be the transformation of G by E resulting into a point? currently E>>>G is considered as a Reflector." Root cause: `sw(E, G)` is a grade-1 null vector (point), but ganja's Float32 sandwich leaves grade-3 noise at ~1e-8…1e-6 of the dominant grade. `gradeFlags`' relative cutoff was `maxN * 1e-8` — *below* that noise — so grade-3 flagged as present → odd grade-1+grade-3 mix → `reflector` (~86% of generic-coordinate point pairs; axis-aligned cases happened to cancel and read correctly). Fix in `src/algebras/cga310/index.js`: (1) loosened the grade-presence relative threshold to `GRADE_REL = 1e-5` (Float32 has ~1e-7 relative precision, sandwich accumulates several multiplies); (2) made the null-point cutoff in `classifyMV` scale-relative — `nullTol = 1e-6 * (1 + x² + y²)` — since `rSq` has units of length² and a far-from-origin sandwich carries rSq noise that grows with distance, which a fixed `1e-6` mistook for a tiny imaginary round point. Verified: 5000-sample scans now give 0 reflectors, genuine round points (`point(5,5,0.1)`) and genuine reflectors (`e1+e123`) still classify correctly, `point>>>line` still → circle.

94. **CGA `vector(x, y, r)` — ideal round point constructor** — "add vector constructor as ideal round point: V = x·e1 + y·e2 + ½(x²+y²+r²)·einf (just without e0 component)." New draggable CGA primitive. `vector2D(x, y, r=0)` in `cga310/index.js` builds the round-point embedding with the e0 weight dropped (grade-1, w=0 → classifies as `idealPoint`); `toIdealVector` returns its e1/e2 direction. `getRenderPlan` now maps `idealPoint` → `positionedVector {vx: e1, vy: e2}`, so it draws as an arrow from the origin to (x,y) (r lives invisibly in the einf coeff). New `freeVector` node type: parser (`parseExpression.js`) accepts `vector(x,y[,r])` ahead of the generic `vector` branch, gated by `accepts('freeVector')` so VGA/PGA keep the old `{vx,vy}` vector; `nodeTypes.js` registers `freeVector` (guarded by `algebra.vector2D`) producing the MV; `useGraph.js` adds `updateFreeVector` (tip-drag rewrites `vector(x,y[,r])`, preserving r); `Canvas.jsx` adds a `freeVector` tip hit-test (`freeVectorTip`, tail from `vectorPositions`, default origin; `continue` so no tail-grab), drag handler, and `tipDraggable` for freeVector. `'freeVector'` added to CGA `SUPPORTED_NODE_TYPES`; `vector2D`/`toIdealVector` exported in spec. Chose arrow-from-origin rendering (asked). Verified: parse→compute→classify→renderPlan for `vector(2,1,0.5)` (e3=e4=2.625, idealPoint, arrow to (2,1)); VGA `vector(-1,2.5)` still emits old `vector` type; `V^einf` composes (→ idealFlatPoint); production build clean.

95. **CGA `vector(x,y,r)` — draggable tail + round-point radius circle** — "the tail point of the vector should be draggable; the tail point should be rendered like a round point with the circle radius." The ideal round point's tail (its draw anchor) is now a second drag handle and shows its radius. `getRenderPlan`'s `idealPoint` case now also returns `rSq = 2·einf − (x²+y²)` (recovers r² from the einf coefficient). In `Canvas.jsx`: the `positionedVector` render draws an `SvgCircle` of radius √|rSq| at the tail (dashed when r² < 0) when `plan.rSq !== undefined`, layered under the arrow; the `freeVector` hit-test now also grabs the tail, returning dragType `'vector'` so the existing tail-drag path (`setDrawPos`, with snap-on-drag) moves the anchor while the tip keeps rewriting `vector(x,y,r)`; hover-state derivation treats `'freeVectorTip'` like `'vectorTip'` so hovering the tip no longer animates the tail. Tip drag stays relative to the tail (`x − pos.x`), so moving the tail and moving the tip are independent. Verified: rSq extraction (`vector(2,1,0.5)`→0.25, `(2,1,1.2)`→1.44, `(3,-1,0)`→0 ⇒ no circle); production build clean.

96. **CGA ideal point pair + ideal line classification** — "V1 ^ V2 should be an ideal point pair; V1^V2^V3 should be the ideal line (e12inf)" (V1,V2,V3 = `vector(x,y,r)` ideal round points). Previously `V1^V2` mislabeled as a generic `pointPair` (and rendered nothing because the standard extraction degenerates — `pp·einf=0`), and `V1^V2^V3` mislabeled as a finite `line` (its dual degenerates so it also drew nothing). Added the unifying test `dotEinfVanishes(X)` = ‖X·einf‖ < 1e-5·‖X‖ (relative, Float32-safe): a wedge of ideal points has zero inner product with einf. `classifyMV` in `cga310/index.js` now returns `idealPointPair` (grade-2, not flat point, einf-null) and `idealLine` (grade-3, einf-null = e12∧einf, the line at infinity) ahead of the finite line/circle split. New `KIND_COLOR.idealLine`/`idealPointPair` (amber, matching the other ideal kinds); `ExpressionPanel` gets an `idealPointPair` descriptor + `KIND_LABELS` entry (`idealLine` already existed via PGA). Both return `null` from `getRenderPlan` (no canvas glyph — they're objects at infinity with no unique finite drawing; this is strictly better than the prior bogus/empty render). Verified: V1^V2→idealPointPair, V1^V2^V3→idealLine, finite pair/line/circle classifications unchanged; build clean.

97. **CGA ideal line rendered as the boundary ellipse + ideal-point markers** — "L = e1^e2^einf is the ideal line; render it as an ellipse around the canvas like PGA, and show the ideal points on it like PGA." The PGA ideal-line machinery in `Canvas.jsx` is algebra-agnostic — `hasIdealLine` (any visible node classifying as `idealLine`), the `SvgIdealLine` dashed boundary ellipse (case `idealLine`), and the `SvgIdealPointMarker` drawn on every `positionedVector` when `hasIdealLine` — so the only missing link was that CGA's `getRenderPlan` returned `null` for the `idealLine` kind. Added `case 'idealLine': return { kind: 'idealLine' }`. Now `e1^e2^einf` (= `e12inf` = e123+e124) and `V1^V2^V3` draw the screen-space ellipse, and CGA ideal points (`vector(x,y,r)` → `positionedVector`) automatically get a marker on the ellipse boundary in their (x,y) direction, matching PGA. Updated the stale "PGA-only" comment on `hasIdealLine`. Verified: `e1^e2^einf`→ raw e123=e124=1, classify idealLine, plan `{kind:'idealLine'}`; build clean.

98. **Object constructors usable inline in expressions** — "point(-4.41, 2.42) ^ einf should work; more generally object constructors should be usable in expressions." Two fixes: (1) `evalMVArith.js` now dispatches `point`/`flatPoint`/`vector`/`line` as inline constructors — a `CONSTRUCTORS` map built from whatever typed constructors the algebra exposes (`point2D`/`flatPoint2D`/`vector2D`/`line2D`), with args coerced to scalars (`toScalarArg`), dispatched in `parseFactor` ahead of builtins/user-fns; constructor names are added to the `extractMVDeps` exclusion list (alongside builtins/consts/blades) so `point` isn't mistaken for a variable while real args like `a` in `point(a,2)` still register as deps. (2) `parseExpression.js`: `parse2DCall`/`parse3Call` only checked `startsWith('point(') && endsWith(')')`, so `point(1,0) ^ point(-1,0.5)` (ends in `)`) was mis-captured as a `freePoint` with garbage coords (`yExpr="0) ^ point(-1, 0.5"`). Added a `balancedParens(inner)` guard so a call only matches when it spans the whole expression; otherwise it falls through to `mvExpr` → `evalMVArith`. This also fixed a latent PGA bug — `point(1,2) & point(3,4)` now routes to `joinLine` instead of a broken `freePoint`. Verified: CGA `point^point`→pointPair, `point^point^point`→circle, `point(...)^einf`→flatPoint, `flatPoint(...)^e1`→line, `point(2,3,0.5)`→roundPoint, `point(a,2)`→deps `['a']`; top-level `point(1,2)` still `freePoint` (draggable); build + lint clean.

99. **CGA zero-radius circle (point circle) drawn as a dot** — `C = !P` (dual of a point) is a circle of radius 0; it classified as `circle` but with r≈0.0005 (Float32 noise) so it drew a sub-pixel, invisible ring. `extractCircle` in `cga310/index.js` now treats `|r²| < 1e-6·(1+cx²+cy²)` (relative cutoff, absorbs the distance-scaled noise) as exactly zero and returns `{cx, cy, r: 0}`. `Canvas.jsx` circle render case: when `plan.r === 0`, draw a filled dot (radius 4·weight) in the circle color instead of `SvgCircle` (fixed screen size → visible at any zoom, and the red circle-color keeps it distinct from a blue point). `ExpressionPanel` shows "Point circle (x, y) r=0". Chose a plain dot per user preference ("maybe just a dot?"). Verified: `!point(-2.23,2.78)`→ r=0 at (-2.23,2.78); genuine unit circle still r=1; build clean.

100. **CGA conformal/null-basis blade names (e01, e10inf, einf120, …)** — "add all blades." e0 and einf aren't basis blades (they're e3/e4 combinations), so names mixing them with the Euclidean basis are MV combinations, not single index+sign. Added `bladeNameToMV(name)` in `cga310/index.js`: tokenizes the suffix into factors from {0,1,2,3,4,inf} (greedy `inf` before digits) and wedges the corresponding factor vectors in order — the wedge yields the right sign for reorderings (`e10 = −e01`) and zero for repeats (`e00 = 0`). Exported in spec. `evalMVArith.js` destructures `bladeNameToMV` and uses it as the bare-identifier resolution fallback (after `fullEnv`/`parseBladeName`), and adds it to the `extractMVDeps` exclusion list so blade names aren't mistaken for variables (real args still register: deps of `2e01 + a*e12inf` = `['a']`). Expressions with these blades route through `mvExpr`→`evalMVArith` automatically (parseMVExpr can't represent multi-component blades, so it falls through). Verified: `e01`=(e13−e14)/2, `e0inf`=`E0INF`, `e12inf`=`einf12`=ideal-line blade, `einf120`→e1234, `3*e01+e12` composes; build/lint clean. (Coefficient *extraction* `A.e01` not added — names resolve as values only.)

101. **CGA display basis: `einf` → `inf`** — In the conformal display basis (`DISPLAY_BLADE_NAMES`, used read-only by `ExpressionPanel` when "display basis" is on), renamed the `einf` token to `inf` everywhere: `einf`→`inf`, `e1einf`→`e1inf`, `e2einf`→`e2inf`, `e0einf`→`e0inf`, `e12einf`→`e12inf`, `e1e0einf`→`e1e0inf`, `e2e0einf`→`e2e0inf`, `e12e0einf`→`e12e0inf`. Cosmetic only (names render the blade-sum string; never parsed back). Updated matching `toDisplayCoeffs` comments. Build clean.

102. **CGA ideal point pair (e12) rendered as an oriented loop** — "how should e12 be rendered?" e12 is the oriented unit area / generator of rotation about the origin; it classifies as `idealPointPair` and previously drew nothing. `getRenderPlan` now returns `{ kind: 'bivector', value: val[5] }` (the e12 coefficient) for `idealPointPair`, so the Canvas reuses VGA's `SvgBivector` glyph — a fixed-radius oriented loop at the origin, CCW for +e12 / CW for −e12, in the idealPointPair color (amber). Added `idealPointPair` to `ExpressionPanel`'s `DRAWABLE_KINDS` (enables its label UI, matching VGA `bivector`). The e1∞/e2∞ direction part of a general ideal point pair is not drawn (only the area/e12 part). Verified: `e12`→loop value 1, `-e12`→-1, `2*e12`→2, `V1^V2`→1.67; build clean. (User chose the oriented-loop option.)

## 2026-06-02

103. **CGA feature verification + `!(circle)` imaginary bug fix** — "test if all cga features work well." Automated Playwright test across 20 cases (object types, blade names, inline expressions, sandwich, display basis, algebra info modal). 19/20 passed; the one "failure" was a test-script false negative (scalar display uses a different DOM path). Bug found and fixed: `!(circle)` classified as "Imaginary round point" when it should be real. Root cause: `extractRoundPoint` used `rSq = -dotPP / w²` (OPNS convention where P·P = -r²), but ganja's dual gives IPNS grade-1 vectors with w < 0 and S·S = +r² (opposite sign). Fix: `rSq = -dotPP / (w · |w|)`, which equals `-dotPP/w²` for OPNS (w > 0) and `+dotPP/w²` for IPNS (w < 0), handling both conventions correctly. All regression cases pass: null point, round point, negated null point, `!(line)` → ideal point. Changed: `src/algebras/cga310/index.js` (`extractRoundPoint`).

## 2026-06-07

119. **CCGA named conic constructors** — "check Workspace/notebook/ccga; what should we add to our ccga visualizer?" Reviewed the reference catalog (`OBJECTS.md`, `CCGA_cheat_sheet.md`, `transform.py`); the biggest gaps were transformations (versors), named conic constructors, property builtins, and intersection rendering. User chose the **named-conics** ergonomics slice (no classifier rework). Added `circle/ellipse/hyperbola/parabola/tilted_ellipse/line/conic(A..F)` as expression-language constructors. New `conicIPNS(A,B,C,D,E,F)` in `src/algebras/ccga/index.js` writes the grade-1 IPNS `s`-vector Mul-free (orthogonal-slot expansion of `s = -2A·eo1-2B·eo2-C·eo3+D·e1+E·e2-(F/2)(einf1+einf2)`, round-trips with the existing `coeffsFromGrade1`); the named builders map shape params → (A..F) per OBJECTS.md §4. A **circle** lands as a round point (existing roundPoint render = a circle); ellipse/hyperbola/parabola/tilted classify as `conic` (SvgConic); **`line` returns the grade-7 dual** because a bare grade-1 IPNS line has zero einf-weight and would otherwise read as an ideal-point arrow (#117). Wiring: a generic `algebra.namedConstructors` map is `Object.assign`-ed into evalMVArith's `CONSTRUCTORS` (no CCGA names hardcoded in the shared evaluator — the existing `toScalarArg`/multi-arg dispatch already handles up to 6 args + reactivity), and the names are added to `parseExpression`'s shared `BUILTIN_CONSTRUCTOR_NAMES` (alongside the CCGA-only `vinf`). Documented in the ExpressionPanel help table; added `saved_graphs/ccga_named_conics.json` (circle + animated `ellipse(a,…)` + hyperbola + line + tilted ellipse). Verified headlessly: every constructor classifies/renders as intended, exact incidence on axis-aligned and tilted ellipses (residual ~1e-7), `circle(0,0,t)` registers dep `['t']`; build + lint clean.

118. **CCGA conic-graph drag lag — memoize classify/render per value** — "ccga performance on some saved graph are very bad. inspect it and propose optimization." Profiled ganja's `Algebra(5,3)` products: `Mul`/`Dual`/`Length` are dense 256-dim ≈ **2.1–2.5 ms each** (`Wedge`/`Dot`/`Vee` are sparse, 0.2–0.5 ms; `Reverse` ~0). The lag wasn't the graphs' own ops but **redundant re-classification**: `conicCoeffs()` hides a 2.5 ms `Mul(val, Iinv)`, and `classifyMV` runs ~7× per value per render (positions, colorMap, vectorPositions, labelMap, idealLine check, Canvas, ExpressionPanel) plus `getRenderPlan` re-runs both classify *and* a second `conicCoeffs`. Measured ≈ **19 ms per conic per tick** → `ccga_general_conic`/`ccga_polar_tangent`/`ccga_axes_foci` (many MV nodes) stack to 50–100 ms/tick. Fix (localized to `src/algebras/ccga/index.js`): MV values are immutable Float32Arrays created fresh each evaluation and shared by reference across consumers, so added `WeakMap` caches keyed on the value object for `classifyMV` and `getRenderPlan` (`classifyImpl`/`renderPlanImpl` do the real work; old entries GC on the next eval). Also stashed the conic `geom` on the classify result so `getRenderPlan` reuses it instead of recomputing the dual. Result: 19 ms → ~0 ms when a value is unchanged across consumers, and **19 ms → 3.6 ms** in the realistic drag case (defining point moved ⇒ conic rebuilt once/tick). No behavior change — all classifications verified unchanged (`point`/`roundPoint`/`vector`/`vinf`/conic ellipse/dipole/flat point/`!conic`); build + lint clean. Noted but deferred: a fast `!`/Dual via a precomputed sign+permutation table (Dual is 2.1 ms; building the table costs 256 probes ≈ 0.5 s at load) and scalar-mul shortcut (ganja already special-cases numeric scalars, so `1*point` is cheap).

## 2026-06-06

105. **CCGA drag perf + null-basis display** — "the app freeze and lag a lot... dragging the point is not fluid; also add an option to print mv expression in the null basis (just like CGA)." Root cause: every `point2D`/`classifyMV`/`objectWeight` went through ganja's 256-dim geometric product (`Mul` is O(256²) ≈ 2 ms each regardless of sparsity), so each point cost ~16 ms/tick. Fixed by computing the point hot paths with **direct component arithmetic** (closed-form embedding `[3]=1−x²/4 … [8]=xy/2`; origin weight `w=(p3+p4+p6+p7)/4`; square via a precomputed per-blade `BLADE_SQUARE` table; `objectWeight` = plain L2). 1000 point+classify+render now ≈ 11 ms total (~1400× faster) → fluid dragging. Added the conformal **null-basis display** (`displayBladeNames` + `toDisplayCoeffs`, the CCGA analog of CGA's e0/einf view): a change of basis on the 8 generators extended to all 256 blades once at load via symbolic exterior multiplication, so per-render it's a sparse dot. A point reads as `x·e1 + y·e2 + eo1 + eo2 + ½x²·einf1 + ½y²·einf2 + xy·einf3`. The panel's existing "display basis" toggle picks it up automatically.

106. **CCGA dipole + flat point** — "add dipole object pp = p1 ^ p2 and flat point (p1 ^ Iinf); check the doc for the decompose-into-two-points formula." Followed `notebook/ccga` (`point_pairs.ipynb` `recover`, `extract.py`, `objects.py`). Dipole (grade-2): `m = −(einf⌋pp)`, `inv = m/(m·m)`, `P± = normalize((pp ± √(pp²))·inv)` — `classifyMV` grade-2 → `pointPair`, rendered via the shared `SvgPointPair` (two dots + dashed segment). Flat point `p∧Iinf` (grade-4) collapses to `eo∧Iinf + x·(e1∧Iinf) + y·(e2∧Iinf)`, so (x,y) read off by component ratio (Mul-free) → `flatPoint` kind, shared diamond render. Optimized the dipole render path from 5 Muls to 2 (Mul-free scalar squares via `BLADE_SQUARE`; `(pp±√pp²)·inv = pp·inv ± √·inv` needs a single `Mul(pp,inv)`). Showcase now: P1,P2 + `pp = P1^P2` + P3 + `F = P3^Iinf`. Build + lint clean.

107. **CCGA `sqrt` of a 2-blade square + dipole-extraction division lag** — "(PP + sqrt(PP*PP))/(einf|PP) does not work — sqrt(PP*PP) does not return a scalar," then "the ccga_dipole saved graph is laggy when dragging." Two fixes in `src/graph/evalMVArith.js` (both general, all algebras): (1) `sqrt` of a multivector used an *absolute* 1e-10 cutoff to decide "is this scalar?"; a 2-blade's square is a pure scalar mathematically but carries ~1e-6 Float32 grade noise, so it fell into the motor `Log/Exp` branch and never returned a scalar — switched to a *relative* cutoff (`maxNon < |scalar|·1e-4`), so `sqrt(pp*pp)` returns `√(scalar part)`. (2) Drag lag was 38 ms/tick; profiling pinned it entirely on `m.Inverse` (ganja's general 256-dim inverse ≈ **30 ms**) inside the `/(einf|PP)` division. Added a versor fast path to `/`: for a blade/versor B (here a vector), `B⁻¹ = ~B/(B·~B)` since `B·~B` is scalar — one product instead of ~15. Drag dropped to ~9 ms/tick (4×), endpoints still recovered exactly. Build + lint clean.

108. **CCGA grade-7 conic through 5 points** — "look at ccga_conic_construction — build the grade-7 conic through the 5 points, with the dual form linking directly to the conic coefficients." Implemented the conics slice. Adapter (`src/algebras/ccga/index.js`): `conicCoeffs(C)` dualizes the grade-7 OPNS conic via explicit `Mul(C, Iinv)` (notebook convention) and reads the grade-1 IPNS coefficients `A=−(c3+c6)/4, B=−(c4+c7)/4, C=−(c5+c8)/2, D=c1, E=c2, F=(c3−c6)+(c4−c7)` (classify.py); `conicGeometry` reduces (A..F) to subtype (Δ=C²−4AB) + drawable form (centre + rotated principal semi-axes for central conics; raw coeffs for parabola/line). `classifyMV` grade-7 → `conic`; `getRenderPlan` → `{kind:'conic', subtype, …geom}`. New `SvgConic` in `Canvas.jsx` (per-type parametric, as chosen in planning): ellipse/circle → crisp rotated `<ellipse>`, hyperbola → two sampled `cosh/sinh` branches, parabola → sampled arc, line → `SvgLine`; sampling extent from the viewport. ExpressionPanel: `conic` descriptor (shows subtype + centre/axes) + KIND_LABELS + DRAWABLE_KINDS. Verified the 5 saved points lie on the conic (residuals ~1e-7) → classified ellipse, centre (−0.89, 0.46); drag of a defining point ≈ 6 ms/tick. Build clean; lint adds no new errors.

109. **CCGA: dual of a conic must not classify as a point** — "the dual of a conic should not be considered as a point because it contains an eo3 blade." `classifyMV` treated every pure grade-1 vector as a point, but a grade-1 IPNS conic (the dual of a grade-7 conic) carries eo3 / eōbar components that a true point cannot (notebook §3.5: points span only V₆, not the W₂ gauge directions e_ō, e_o3). Added `isPointVector(v)` — a grade-1 vector is a point only if `eo3=(v5+v8)/2 ≈ 0` and `eōbar=eo1−eo2 ≈ 0` (relative). The grade-1 branch now routes non-point vectors to `kind:'conic'`. Generalized `conicCoeffs` to read coefficients directly from a grade-1 IPNS (no dual) or dualize a grade-7 OPNS first, so both `C = E^…^J^Iod` and its dual `!C` render the same ellipse. Genuine points/round points (and a circle's dual, which has balanced eo and no eo3) still classify correctly. Build + lint clean.

110. **CCGA `vector(x,y[,r])` — ideal round point** — "add vector like in CGA (same expression as the point but without the homogeneous coordinate eo1+eo2)." Added `vector2D(x,y,r=0)` = the Veronese point embedding with the `eo` (=eo1+eo2) part dropped (direct component writes, Mul-free) → grade-1 with zero origin weight → classifies as `idealPoint`. Added `toIdealVector`, an `idealPoint` → `positionedVector` render case (arrow from origin; r² recovered from the einf parts → tail circle), and `'freeVector'` to `SUPPORTED_NODE_TYPES`. The parser/nodeTypes/Canvas `freeVector` path (drag tip+tail, arrow render) is generic and already present from CGA, so it lit up by exposing `vector2D`. `vector(1.5,0.8)` draws an arrow; `vector(2,1,0.5)` adds the radius circle; `point(…)` still keeps `eo`. Build + lint clean.

111. **CCGA reality must be scale-invariant (negation bug)** — user noted reality = sign(p²) is scale-multiple invariant (p→λp ⇒ λ²p²), with invariant radius r² = p²/(p·einf)². My `extractRoundPoint` had copied CGA's `rSq = p²/(w·|w|)` (w = −(p·einf)), whose `w·|w| = sign(w)·w²` flips sign under p→−p — so negating a real round point wrongly read it as imaginary. CGA needs that trick because its OPNS point (P²=−r², w>0) and IPNS sphere dual (S²=+r², w<0) carry opposite signs; CCGA has no such duality (a round point *is* the circle dual, p²=+r²), so the correct denominator is `w²`. Changed to `rSq = p²/w²` (= p²/(p·einf)²). Verified r² is constant across λ∈{1,−1,2,−3,0.5} for real (r²=4) and imaginary (r²=−2.25) points; null points stay null — mirrors the Python `test_reality_is_scale_invariant`. Build + lint clean.

112. **CCGA point at infinity (Veronese limit) as a distinct ideal type** — "model the ideal point of the form ½x²·einf1 + ½y²·einf2 + xy·einf3 as a different type of ideal point." This pure-quadratic form (no eo, NO linear e1/e2) is the notebook's `point_at_infinity` (vs `make_ideal_point`/`vector(x,y)`, which keeps e1/e2). `classifyMV` now splits the w≈0 ideal case: a linear e1/e2 part ⇒ `idealPoint` (vector); purely quadratic ⇒ new kind `infinityPoint`. Added `infinityDir(v)` recovering the asymptotic direction (vx²=2(v6−v3), vy²=2(v7−v4), sign from einf3=v8−v5), an `infinityPoint`→`positionedVector` render (arrow in that direction), `KIND_COLOR`, and ExpressionPanel label/descriptor ("Point at infinity → (vx, vy)") + DRAWABLE_KINDS. `vinf(2,1)`→infinityPoint, while `vector(2,1)`/`point(2,1)` are unchanged. Build clean; lint adds no new errors.

113. **CCGA `vinf(vx,vy)` constructor (point at infinity)** — "add a constructor for this new object." Added `infinityPoint2D(vx,vy)` (= vector2D minus the linear e1/e2 part → purely quadratic ½vx²·einf1+½vy²·einf2+vx·vy·einf3), exposed in the spec, and wired as an inline constructor `vinf` (evalMVArith `CONSTRUCTORS.vinf`, plus `vinf` in parseExpression's BUILTIN_CONSTRUCTOR_NAMES). Works top-level (`I = vinf(2,1)` → infinityPoint) and inside expressions with reactive deps (`vinf(a,1)`). Validated semantics inline: `P1^P2^P3^P4^vinf(1,1)^Iod` → parabola (a conic through a point at infinity is tangent to the line at infinity). Chose the short notebook name `vinf`. Build + lint clean.

104. **CCGA plan + first slice (points / round points)** — "I would like to add the CCGA algebra" then "Lets first just add the round point on CCGA and then add other objects." Researched the Conic Conformal GA references (`notebook/ccga`, Obsidian notes) and wrote an implementation plan to `docs/plans/ccga.md`. De-risked ganja viability: `Algebra(5,3)` defaults to a *graded* representation that breaks the flat-array contract and crashes `Dual`; **`Algebra({p:5,q:3,graded:false})` forces a working 256-element flat array** (correct metric e1–e5→+1/e6–e8→−1, `I²=−1`, `eo_i·einf_i=−1`). Validated the full math port in throwaway JS (grade-7 conic `Iod∧p1∧…∧p5` → `Mul(C, I_inv)` dual → A..F coefficients → discriminant recovers circle/ellipse/hyperbola/parabola exactly). First slice shipped: new `src/algebras/ccga/index.js` (flat 256-dim algebra; basis metadata generated from `Algebra.describe().basis`; null basis + special blades eo/einf/Iod/I/Iinv… exposed as `mvConsts`; `point2D(x,y,r)` Veronese embedding; `classifyMV` for grade-1 null/round/ideal points; `getRenderPlan`/`toEuclidean`/`hasDepPointCoeffs` reusing the CGA finitePoint/roundPoint render + drag path). Registered in `src/algebras/index.js`. Guarded the Algebra Info modal against the 140 s, 256×256 Cayley sweep: `cayley.js` gains `MAX_CAYLEY_DIM=64` and a direct-diagonal `basisSquares`; `AlgebraInfoModal.jsx` omits the table (with a note) past the cap. Showcase: draggable null point `A = point(1, 0.5)` + round point `R = point(-1, 0, 1.2)`. Build clean; lint adds no new errors. Higher-grade objects (conics, point pairs) deferred to later slices.

114. **`**n` power operator** — "add ** operator as the square of the element." Added an integer power operator to the general MV arithmetic evaluator (`evalMVArith.js`): tokenizer emits a `**` op (before single-char `*`), validator accepts it as a tight postfix (optional `-`, then a numeric literal — mirroring the existing `^-1` inverse postfix), and `parseFactor`'s postfix loop dispatches to a new `applyPow(val, n)` helper. Semantics: `A**2 = A*A` (repeated `Algebra.Mul`), `A**0 = 1` (scalar), `A**-n = (A.inverse)**n`, pure numbers use `Math.pow` (any real exponent), maps over lists; binds tighter than `*` so `2*A**2 = 2*(A²)` and `-A**2 = -(A²)`. Documented in the ExpressionPanel help table. Build/HMR clean.

115. **Drag control for CCGA point at infinity** — "add drag control for point at infinity (just like ideal point)." Made `vinf(x, y)` a first-class draggable node type `freeInfinityPoint`, mirroring the ideal-point `freeVector` path: new node type in `nodeTypes.js` (computes via `infinityPoint2D`), a `vinf(x,y)` parse branch in `parseExpression.js`, `'freeInfinityPoint'` added to CCGA `SUPPORTED_NODE_TYPES`, `updateFreeInfinityPoint` in `useGraph.js` (tip drag rewrites `vinf(x,y)`), and Canvas plumbing — destructure + snap, a hit-test block (tip only, anchored at origin → `freeInfinityPointTip`), the drag handler, `tipDraggable`/tip-hover inclusion, and the unlabeled-drag guard (`valKind !== 'infinityPoint'`). Help-table row added. Note: a point at infinity is direction-up-to-sign (the embedding is purely quadratic), so `infinityDir` returns the vx≥0 representative — dragging follows the cursor in the right half-plane and mirrors to the antipodal ray in the left, which is the correct (sign-ambiguous) object. `vinf` still works inline (`p ^ vinf(1,1)`) since the evalMVArith constructor is unchanged. Build/HMR clean; changed files add no new lint errors.

116. **Point-at-infinity drags freely (signed source direction)** — "the vector coordinate should be normal x,y then compute its multivector value (for now ... the square the vector cannot be moved freely)." The arrow direction was recovered from the squared MV via `infinityDir`, which returns the vx≥0 representative — so the tip couldn't follow the cursor into the left/lower half-planes. Flipped the data flow: `infinityPoint2D(vx, vy)` now stashes the signed source coords as `v.dir = { vx, vy }`, and `getRenderPlan`'s infinityPoint case prefers `val.dir ?? infinityDir(val)`. A dragged `vinf(x, y)` now renders its tip exactly at (x, y) and moves freely; derived MVs (products, sandwiches, normalized values) drop `.dir` and fall back to `infinityDir`. No key-iteration/spread/serialization touches MV values, so the expando property is safe. Build/HMR + lint clean.

117. **CCGA showcase graphs (pencil, intersection, type-morph, polar, center, axes/foci)** — "Propose some graph to implement ccga feature" → built 6 DSL-only `saved_graphs/ccga_*.json` (no adapter changes). `ccga_pencil` (slider `t` sweeps `(1-t)C1+t C2` through 4 shared base points); `ccga_intersection` (constructive: 4 draggable points + two conics crossing there; `I4=C1 & C2` kept hidden since the grade-6 meet isn't drawable yet); `ccga_conic_type_morph` (single slider `k`, `B=(einf1+k*einf2)^einf3` morphs hyperbola k>0 → parabola k=0 → ellipse k<0); `ccga_polar_tangent` (GA-native polar via probe-vector contraction `pol=0.5(s|gx)e1+0.5(s|gy)e2-(s|gw)einf`, drawn as `!pol`; tangent when `q` on `C`); `ccga_center` (center as `point(cx,cy)` from the `conic_center` inner-product formula + Δ₂=det/4); `ccga_axes_foci` (trig-free principal axis + foci from the deviatoric spin-2 vector `(s|einfb, s|einf3)` via half-angle `u∝(δ+dx,dy)` — avoids `atan2`/`cos`/`sin`, which don't accept `|`-derived MV-scalars in the DSL). Validated every graph headlessly through `spec.evalMVArith` (all visible rows classify as intended) and cross-checked `ccga_center`/`ccga_axes_foci` numerically against the Python notebook (`conic_center`/`conic_axes`/`conic_foci` match exactly). Gotchas found: the inline ideal-point constructor is `vinf` (not `infinityPoint`); grade-1 IPNS lines render only after `!` (grade-7 `line` subtype); a conic∨conic meet (grade-6) is not yet drawable — deferred as a future adapter feature.

118. **CCGA lag (ccga_degenerate_conic_start)** — "look at the saved graph ccga_degenerate_conic_start it's very laggy atm." Profiled: cost is the Cl(5,3) 256-dim ganja products in the per-drag `evaluate()` (~10ms warm / ~22ms cold). Two fixes. (1) Engine, `src/graph/evalMVArith.js`: the per-call `fullEnv` spread rebuilt a ~255-key object (all basis blades) on every — deeply recursive, user-function-heavy — call; replaced with a precomputed `CONST_ENV` layered via `Object.create` + own-key copy of the call env, and added a `tokenize` memo cache (bodies/expressions are constant strings re-eval'd every tick). Behavior-preserving (fullEnv is lookup-only, never iterated; token arrays are read-only during parse). ~5% across all CCGA graphs. (2) Graph: `det3` was invoked 8× with only 4 distinct args (`det3(sC1±sC2)` each computed twice across B and C; `det3(sC1)`/`det3(sC2)` duplicated by D/B and A/C). Added `P=det3(sC1+sC2)`, `M=det3(sC1-sC2)`; rewrote `B=0.5*(P+M)-D`, `C=0.5*(P+M)-A` — identical `det0`, ~15-20% fewer products. Dropped the stale base64 `hash` (loader uses `items`; share-URL re-encodes). Remaining ~5ms is the two 6-point wedge chains for C1/C2 (inherent — a sparse GA-product fast path would be the next lever but is a larger, riskier change).

119. **CCGA lag, real root cause: scalar×blade went through dense Mul** — "dragging points is still very laggy ... if I remove the functions the drag is fluid." Measured single ganja ops in Cl(5,3): `Mul`/`Dual` ≈ 76µs, `Wedge`/`Vee` ≈ 15µs, `Dot` ≈ 21µs. The function bodies rebuild lines from scalar coefficients — `a(C)*e1`, `0.5*c(C)*e2`, … — where `a(C)…f(C)` are grade-0 (pure scalar) MVs. The `*` operator in `src/graph/evalMVArith.js` only fast-pathed *literal* numbers (`lNum`/`rNum`); a scalar-valued **MV** fell through to the full 256-dim `Algebra.Mul` (~77µs each), ~9 per `det3`. Added an `isPureScalarMV` check mirroring the existing `/`-operator fast path: if either factor is grade-0, use `scaleMV` instead of `Mul`. End-to-end full-graph eval: **16.2ms → 9.7ms/eval (~40%)** combined with the entry-118 fixes; the scalar fast path alone is ~34%. Verified mathematically (scalar commutes/scales in any GA) and numerically across VGA/CCGA/PGA (scalar×blade scales, `e1*e2=e12` unaffected, PGA degenerate `e0|e0=0` still routes correctly). Remaining cost is Duals (~14×76µs) + the C1/C2 high-grade wedge chains + grade-1|grade-1 dots — a sparse/permutation Dual and a grade-1 dot fast path would be the next levers but are riskier adapter-level changes.

120. **CCGA `sqrt3` builtin + degenerate-conic construction (normalization, Float64, omg fix)** — "add a sqrt3 and check the importance of conic normalization for the degenerate construction to work cleanly." (a) Added `sqrt3` (real cube root) to `src/graph/evalMVArith.js`: in `BUILTIN_FN_NAMES` + a dispatch branch mirroring `sqrt` but with `Math.cbrt` and `log/3` for the versor case — single-valued over reals so no sign-flip, which is exactly what lets it take the (possibly negative) Cardano radicand. (b) Normalization: proved it's required, not cosmetic — ganja MVs are Float32 and the Cardano chain raises ‖sC‖ to powers up to 18 (`det1²∝‖sC‖¹⁸`); the raw 6-point wedge sits at ‖sC‖≈0.1 → `det1²≈6e-37`, ~8 orders above the Float32 underflow floor, and shrinking the conic ~3× underflows `det1²`→0 → NaN. The graph's `normalizeMode:"norm"` on C1/C2 pins ‖sC‖≈O(1) and is essentially mandatory. (c) Switched CCGA to **Float64** via `baseType:Float64Array` (ganja supports it for flat generators) — `src/algebras/ccga/index.js`; exact base-point incidence (1e-16), ~280 orders more underflow headroom, ~neutral perf, 2× MV memory. (d) Found and fixed the actual blocker — a missing `0.5` in the graph's Cardano cube-root arg: `sqrt3(0.5*det1 ± sqrt(rad))` must be `sqrt3(0.5*(det1 ± sqrt(rad)))`; residual `det3(Cdeg)/‖Cdeg‖³` went 9.5e-6 → **4.77e-19** (machine-clean degenerate conic through all 4 base points). Validated the whole construction against `notebook/ccga/ccga/extract.py:_resolvent_roots`. Earlier-flagged graph typos (C difference-formula, `d`→`D`, `omega`→`omg`) were already fixed by the user. eslint clean.

121. **CCGA degenerate-conic classification (Δ₂/Δ₃, paper Table 1)** — "add classification of degenerate conic using det2 and det3 as in the paper." The CCGA classifier only used the quadratic discriminant Δ=c²−4ab, never Δ₃, so degenerate conics drew as ordinary ellipses/hyperbolas. Added to `conicGeometry` (`src/algebras/ccga/index.js`): Δ₃ = det(Hessian) = abf+(cde−c²f−bd²−ae²)/4 with the notebook's scale-invariant cutoff |Δ₃|<1e-7·max(|a..f|)³ (`classify.py:conic_is_degenerate`); when degenerate, Δ₂ sign (via the existing `disc`) splits into `point` (Δ₂>0), `parallelLines` (Δ₂≈0), `linePair` (Δ₂<0) — paper Table 1. New `factorLinePair` helper splits a degenerate conic into its two homogeneous lines via the Hessian-adjugate method (Richter-Gebert / paper Alg. 2), verified on xy, x²−1, x²−y², (x−1)(y−2). Renderer (`SvgConic` in `Canvas.jsx`): `point`→SvgPoint, `linePair`/`parallelLines`→two SvgLines (reusing the homogeneous-line→base+direction conversion). Panel labels (`ExpressionPanel.jsx`): "Degenerate conic — point/two intersecting lines/parallel lines". Non-degenerate ellipse/hyperbola/parabola unchanged; no new lint errors. **Flagged but out of scope:** the `ccga_find_degenerate_conic` graph's own `det3` is broken — its a..f extraction contracts with the null `eo_i` (square 0) instead of the dual `einf_i`, giving a=b, c≡0, det3≠Δ₃, so its `Cdeg` isn't actually degenerate (true Δ₃/s³≈−0.01) and will correctly classify as an ellipse until that graph is repaired.

122. **CCGA degenerate-finder: casus irreducibilis fix (trig/Viète root)** — "D=det3(sC1) seems to be always zero?" → traced the `ccga_find_degenerate_conic` algorithm. Two things had since been fixed in the graph by the user (extraction now `a=0.5*s|einf1 … f=2*s|eo1`, det3 sign-flipped) so `det3 = true Δ₃` exactly (ratio 1.0000). Remaining bug = **casus irreducibilis**: for 4 real base points the resolvent cubic has 3 real roots ⇒ `det1²−4det0³ < 0` (here −1.09e-4) ⇒ `sqrt(neg)=NaN` (in-graph the radical collapsed, giving a wrong real root t≈−1.834 vs true −1.866). The single-radical Cardano form is invalid exactly in the case the construction exists for. Replaced `omg_±/λ/μ` with the real trig (Viète) form: `phi=acos(det1/(2*det0*sqrt(det0)))`, `t=(-1/(3A))(B+2 sqrt(det0) cos(phi/3))`, `Cdeg=sC1+t*sC2` (DSL note: `det0**1.5` is invalid — `**` needs an integer exponent on a multivector — so `det0*sqrt(det0)`). Verified: t=−1.86566 (exact root), `Cdeg` Δ₃/s³≈−1.4e-17 (genuinely degenerate), classifies as `linePair`, renders two lines through all 4 base points. Caveat noted: acos arg can drift outside [−1,1] near a double root; DSL has no clamp (no min/max builtin). Other two pairings available via `cos((phi−2πk)/3)`, k=1,2.

123. **CCGA line-extraction graph (`ccga_extract_lines.json`)** — "how to implement the line extraction as a new saved graph?" The paper's Alg. 2 (`factor_line_pair`) is matrix-based (Hessian adjugate + argmin/argmax) — not expressible in the functional graph DSL. But for a *crossing* line pair (Δ₂<0, the 4-real-points case) the two lines are the asymptotes through the conic centre, which IS pure-DSL: from the extracted `a..f`, centre `cx=(ce/4−bd/2)/Δ₂`, `cy=(cd/4−ae/2)/Δ₂` (paper §6 `l₁∨l₂`), slopes `m=(−c±√(c²−4ab))/(2b)`, lines `line(m,−1, cy−m·cx)`. **No adapter/engine changes.** New graph = `ccga_find_degenerate_conic` (with the trig-form `Cdeg`) + 13 appended nodes (`qa..qe`, `Del2`, `cx/cy`, `dsc`, `sm1/sm2`, `La`/`Lb`). Verified headless: `La`/`Lb` classify as `line`, no NaN, and split the base points exactly — `La` through G,H and `Lb` through E,F (`P∧L≈3e-13`), `Del2<0`. Confirmed the DSL lines equal the adapter's `factorLinePair` output up to scale. Out of scope (noted): point extraction (Alg. 3/4, line∩conic) is coordinate-based (rotor+quadratic), and parallel/vertical-line edge cases.

124. **CCGA degenerate-conic miss: translation-variant degeneracy cutoff** — "look at ccga_degenerate_hyperbola — det3=0, det2=-0.3019, but it's not classified as a line pair." `conicGeometry`'s degeneracy test used `|Δ₃| < 1e-7·cmax³` where `cmax = max(|A|..|F|)`. Δ₃=det(H₃) is translation-invariant (unimodular conjugation) but `cmax` is *not* — `F` grows ∝R² off-origin (and the dual of a 5-point conic is tiny overall, cmax≈7e-4), so `|Δ₃|/cmax³` collapses and a genuine line pair fell through to hyperbola/parabola (here it hit the non-scale-invariant early parabola test → "parabola"). Fix (`src/algebras/ccga/index.js`): normalize by the **quadratic part** `qmax = max(|A|,|B|,|C|)` instead — `|Δ₃|/qmax³` is fully translation-invariant. Measured it: degenerate ≈1.2e-5 at every position (origin, +8, +40); smallest non-degenerate (hyperbola) = 0.107 — a 4-order gap. Set the cutoff to `1e-4·qmax³` (and `dtol` to `1e-3·qmax²` for the linePair/parallelLines split). Verified through the real `classifyMV` across 27 cases (ellipse/hyperbola/parabola/tilt/line/explicit linePair+parallel+double, each at 4 positions incl. R=50): all correct, user's conic → `linePair` at every position. Circles classify as `roundPoint` — confirmed pre-existing (caught by the round-point path before `conicGeometry`), out of scope.

124b. **Threshold too loose: `1e-3` over-flagged a near-degenerate hyperbola** — user replaced the graph's points with a generic config: "det3=-0.0366, the conic doesn't even pass through J, should be a hyperbola not a line pair." That conic is a genuine *thin* hyperbola — all 5 incidences ≈1e-14 (passes through every point) — with relD3 = |Δ₃|/qmax³ = **8.7e-4**, just under the `1e-3` cutoff. The earlier "0.107 smallest non-degenerate" was wrong: a near-degenerate real conic sits much lower. Retuned the cutoff to **`1e-4·qmax³`** — the geometric mean of the truly-degenerate config (relD3≈1.2e-5) and this near-degenerate hyperbola (8.7e-4), the widest log-space margin (~8.5× each side). Re-verified: user's new config → `hyperbola`, original degenerate config → `linePair` (origin + translated), full battery 18/18.

125. **CCGA grade-4 classification: flat point / round point / quadpole** — "E^F^Iod is a Round Point, E^Iinf is a flat point, quadpole E^F^G^H — the app classifies all grade-4 as flat point but they're different types." `classifyImpl` collapsed every pure grade-4 MV to `flatPoint` (TODO admitted quadpoles "come later"), and `getRenderPlan`'s flatPoint case ran `extractFlatPoint` on all of them → bogus markers for round/quad. Found a clean factorization discriminator (probed empirically): for grade-4 `B`, `w(g)=‖B∧g‖/‖B‖` — **flat** ⟺ `w(einf1)=w(einf2)=w(einf3)≈0` (Iinf divides B), else **round** ⟺ `w(Iinfd)≈0` (infinity gauge Iinfd divides B), else **quadpole**; near-origin separations are 0 vs ~0.5–2.8 so `1e-6` is safe. (Round point is `E^F^Iinfd`, not `E^F^Iod` as first stated — user corrected; first impl tested `∧Iod` and so mislabelled `E^F^Iinfd` as quadpole. `∧Iinf` and `∧Iod` read identically for all three and are *not* discriminators.) Added `classifyGrade4(val)` (`src/algebras/ccga/index.js`) returning `flatPoint`/`roundPointDirect`/`quadpole` — used a *distinct* `roundPointDirect` kind (not the grade-1 `roundPoint`) so the render path doesn't run the grade-1 `extractRoundPoint`; uses a local `rawNorm` (not `objectWeight`, which returns `||1` and would never read as zero). New kinds get no `getRenderPlan` case → `default: return null` (undrawn), so flat point still renders and round/quad are correctly labelled but not drawn (removes the bogus rendering). Labels: `KIND_COLOR` + `getDisplayValue` switch ("Round point"/"Quadpole") + `KIND_LABELS` (`src/ExpressionPanel.jsx`). **Scope (confirmed with user): classification + labels only, near-origin robust.** Documented caveat in a code comment: far-from-origin grade-4 objects misclassify — the Veronese coords (∝R²) collapse both wedges to ~0 and no simple normalization recovers it (unlike the conic q-normalization). Verified through the real `classifyMV` at two placements (all 3 correct) + grade-1/grade-2 regressions; eslint clean.

126. **CCGA: `vector()` builds a vinf + draggable position for vinf** — "modify vector to be vinf instead (no longer need vector), and add position to vinf object." User chose: `vector()` should create the vinf object (point at infinity), not the ideal round point. (a) **Alias:** removed `'freeVector'` from CCGA `SUPPORTED_NODE_TYPES` and made the parser's `freeInfinityPoint` block accept `vector` as well as `vinf` (`src/graph/parseExpression.js`: `parse2DCall(expr,'vinf') || parse2DCall(expr,'vector')`). Gated by `accepts('freeInfinityPoint')` (CCGA-only), so PGA/VGA `vector()` is untouched — verified: CCGA `vector(1,2)`/`vinf(.5,-1)` both parse to `freeInfinityPoint` and evaluate to `infinityPoint`/`positionedVector`; PGA `vector()` stays `vector`. (b) **Position:** vinf was anchored at the origin; added `infinityPoint` to the positionable sets so its arrow base gets a draggable draw-position like vectors — `useGraph.js` `vectorPositions` `isVecVal`, `ExpressionPanel.jsx` `hasPosition` gate, and `Canvas.jsx` (freeInfinityPoint hit-test now reads `vectorPositions[id]` for base→tip, tail drags the anchor via the `'vector'` path; `freeInfinityPointTip` drag subtracts the base; generic value-driven anchor path includes `infinityPoint`). Caveat (accepted by user): the 5 CCGA graphs that used `vector(...)` now build vinf objects — vinf has no e1/e2 part, so any `V.e1`/`V.e2` reads in them (e.g. `ccga_line_pair_*`) return 0 and those graphs change behavior. Zero new lint errors (6 pre-existing unused-vars unchanged).

127. **CCGA vinf: rename description to "Ideal point" + radius parameter (±r²·einf)** — "rename it ideal point in the description and add a radius parameter ±r²*einf." (a) **Label:** the infinityPoint object now reads "Ideal point" everywhere — `getDisplayValue` ("Ideal point → (x, y)  r=…"), `KIND_LABELS`, and the `freeInfinityPoint` node label (`src/ExpressionPanel.jsx`, `src/graph/nodeTypes.js`). (b) **Radius:** `infinityPoint2D(vx, vy, r=0)` (`src/algebras/ccga/index.js`) now adds `±r²·einf` to the value — `s = r·|r|` (sign of r ⇒ ±), `v += s·einf` (einf is the module const), and stashes `v.rSq = s` (dropped by derived products, like `v.dir`). Render plan surfaces `rSq`, so the existing `positionedVector` path draws a circle of radius `|r|` at the tip (dashed when r<0 / imaginary). Wired the 3-arg form through the stack: parser `freeInfinityPoint` block tries `parse3Call('vinf'|'vector')` before the 2-arg (`src/graph/parseExpression.js`); the `freeInfinityPoint` compute evaluates `rExpr` (`nodeTypes.js`); the tip-drag rewrite preserves the `r` arg (`useGraph.js updateFreeInfinityPoint`). Verified headless: `vinf(1,2,0.5)`/`vector(1,2,-0.7)` parse with `rExpr`, the added term is exactly `r·|r|·einf` (coeff 0.25 for r=0.5), `rSq` reaches the render plan (negative → imaginary), classification stays `infinityPoint`; 2-arg `vinf(1,2)` unchanged. No new lint errors.

## 2026-06-11

128. **CCGA grade-4 fix: `p1∧p2∧Iinfd` is a point pair, not a round point** — "using Workspace/notebook/ccga/ check what should be improved in the ccga adapter (especially paper)\n\nCurently P^E^Infd is classify as a round point (it should be a point pair)." `classifyGrade4` (added in #125) returned `roundPointDirect` for any grade-4 `B` with `w(Iinfd)≈0`, but per `OBJECTS.md` §7 "CGA round family via Iinfd", `(p1∧p2)∧Iinfd` (grade 4) is the **CGA point pair** — CGA round points are grade 3 (`p∧Iinfd`), one grade down. Also `roundPointDirect` had no `getRenderPlan` case (rendered nothing). Fix (`src/algebras/ccga/index.js`): when `w(Iinfd)<1e-6`, compute `pp = Iod⌊val` (= `cga.cga_blade(O)`, the grade-2 dipole `p1∧p2`); if `‖pp‖` is non-negligible return `{kind:'pointPair', ccgaPair: pp}` (else fall through to `quadpole`). `renderPlanImpl`'s `pointPair` case now calls `extractDipole(cls.ccgaPair ?? val)` — `extractDipole` is scale-invariant under `pp→λpp` (verified: only ratios are used, reality sign and coordinates preserved for any nonzero λ), so the unknown proportionality constant from `Iod⌊O` doesn't matter. Removed the now-unused `roundPointDirect` from `KIND_COLOR` and from `ExpressionPanel.jsx`'s `KIND_LABELS`/switch. Verified end-to-end: `P^E^Iinfd` for `P=point(1.5,0.5)`, `E=point(-1,-1)` → classifies `pointPair`, renders `p1≈(1.5,0.5)`, `p2=(-1,-1)`, `cx=0.25,cy=-0.25,r≈1.4577,imaginary:false`. eslint clean (4 pre-existing unrelated errors in `ExpressionPanel.jsx` confirmed via `git stash`).

129. **CCGA: complete the CGA round family for grades 3 and 5** — "check Workspace/notebook/ccga\n\nwhat should be added next in the ccga adapter?" → user picked "Complete the CGA round family (grades 3/5)", the natural continuation of #128 (grade 4). Per `OBJECTS.md` §7: grade 3 = CGA round point `p∧Iinfd`; grade 5 = CGA circle `p1∧p2∧p3∧Iinfd` or CGA line `p1∧p2∧einf∧Iinfd ≡ −(p1∧p2∧Iinf)`. Both reuse existing extraction code with **no new math**, verified numerically against the real ganja.js `Cl(5,3)` algebra: **grade 3** — `Iod⌊O` (`A.LDot(Iod,val)`) gives a grade-1 result `∝ p`; the existing `extractRoundPoint` is scale-invariant, so `extractRoundPoint(Iod⌊O)` directly recovers `(x,y,rSq)` (verified `{x:1.5,y:0.5,rSq:0}` for `point(1.5,0.5)^Iinfd`, `rSq:4`/`-4` for r=±2, scale-invariant across λ∈{1,5,-3,0.001}). **Grade 5** — `Iod` and `Iinfd` are both grade-2 and grade-2∧grade-2 wedges commute, so `O∧Iod = T∧Iod∧Iinfd` (grade 7), the same shape as the existing general-conic OPNS object — `conicGeometry(conicCoeffs(O∧Iod))` directly gives the circle (verified circumcircle of (1.5,0.5),(-1,-1),(0.5,1.5): `cx=cy=0.0625, r≈1.5026`, exact match vs. independent computation) or the line (verified `D=-3,E=5,F=2`, both points satisfy `D·x+E·y+F=0` matching `Canvas.jsx`'s `SvgConic` line convention) — `conicGeometry` already discriminates `'line'` (A≈B≈C≈0) vs `'circle'`/`'ellipse'` (A≈B) internally, including 3-collinear-points naturally degrading to `'line'`. A bare tripole/pentapole (no `Iinfd` factor, `‖T∧Iinfd‖/‖T‖≈0.55`) fails the gate and stays `'mixed'`. Added `classifyGrade3`/`classifyGrade5` to `src/algebras/ccga/index.js` (mirroring `classifyGrade4`'s style), wired `onlyGrade(g,3)`/`onlyGrade(g,5)` into `classifyImpl`, and updated `renderPlanImpl`'s `finitePoint`/`roundPoint` cases to `cls.ccgaPoint ?? val` (mirroring `pointPair`'s `cls.ccgaPair ?? val` — purely additive, grade-1 callers unaffected). No `KIND_COLOR`/`SUPPORTED_NODE_TYPES`/`namedConstructors`/`INITIAL_ITEMS` changes — both new kinds reuse existing colors (`finitePoint`/`roundPoint`/`conic`). Verified end-to-end via `classifyMV`/`getRenderPlan` on the showcase points. eslint clean.

## 2026-06-12

130. **CCGA: dotted rendering for the CGA round family (grades 3 & 5)** — "CGA object point, line of grade 5 should be drawn in dotted style (they are not complete objects of grade 7)." The grade-3 round points and grade-5 circles/lines (#129) are lower members of the round ladder — special cases of a grade-7 conic — but rendered identically to the full grade-1/grade-7 objects. Tagged `classifyGrade3`/`classifyGrade5` results with `cga:true`, propagated `cls.cga` onto the `finitePoint`/`roundPoint`/`conic` render plans (`src/algebras/ccga/index.js`), and in `Canvas.jsx` made the `conic` and `roundPoint` cases fall back to `'dotted'` when `plan.cga` and the user hasn't picked a stroke style (`strokeStyle ?? (plan.cga ? 'dotted' : null)`; round-point ring after the imaginary→dashed check). `SvgConic` already routes every subtype through `resolveStrokeDash` so circles/lines dot automatically. Grade-1/grade-7 full conics stay solid; explicit per-item stroke style still wins. Build + eslint clean.

131. **CCGA: grade-1 IPNS conics no longer render as conics; named constructors → grade-7** — "-0.0049e1 + 1e2 + 1.456eo1 + 1.456eo2 should not be rendered as a point with a radius, no?" That object is `eo + x·e1 + y·e2` (no einf) — the IPNS of a real circle through the origin (A=B, C=0, F=0); the round-point path read a real radius (= distance from origin) and drew it as a center dot + ring. User's decision: **only grade-7 OPNS conics render; grade-1 IPNS conics are not drawn directly** (dualize with `!` to view). Two changes in `src/algebras/ccga/index.js`: (a) all named constructors now return the grade-7 dual via a new `opnsConic(cA..cF)=CCGA.Dual(conicIPNS(...))` helper — `circle`/`ellipse`/`hyperbola`/`parabola`/`tilted_ellipse`/`conic`/`line` (used `CCGA.Dual`, not the local `A` shadowed by the coeff in ellipse/hyperbola/tilted; verified no real→imaginary flip — all keep correct centre/axes). (b) New `isVeronesePoint(p)` gate: a genuine point/round point has its einf parts locked to position (`einf3 = x·y`, isotropic radius offset `einf1−½x² == einf2−½y²`, tol `1e-6·(1+x²+y²)`); a grade-1 vector that passes `isPointVector` but fails this is an IPNS circle → returns `{kind:'mixed'}` (undrawn). The grade-1 non-point branch (former `conic`) also now returns `mixed`. Verified end-to-end: constructors draw as grade-7 conics with correct geometry; `point(x,y[,r])` real/imaginary/far still draw; vinf/ideal points unaffected; the problem object → `mixed`/NOT DRAWN, and `!problem` → grade-7 circle. Build + eslint clean.

132. **CCGA: "special point" — the pure-position grade-1 vector** — "0.2417e1 - 0.78e2 - 1eo1 - 1eo2 — it should be recognized as a 'special point'." That object is `w·eo + x·e1 + y·e2` (w=−1, pos (−0.2417, 0.78)) with NO einf part and no eo3/eōbar — the *pure-position* point (the flat point's linear core per `GENERAL_FORM.md`/`OBJECTS.md`): a point carrying position but no Veronese quadratic lift. After #131 it fell into the `mixed`/undrawn catch-all. Added a distinct `specialPoint` kind. (a) `src/algebras/ccga/index.js`: new `isSpecialPoint(p)` (point subspace, `einfWeight≠0`, all three einf coeffs `v6−v3,v7−v4,v8−v5 ≈ 0` rel. to `max(|w|,|e1|,|e2|)·1e-5`); in `classifyImpl`'s grade-1 Veronese-inconsistent branch, return `{kind:'specialPoint'}` when `isSpecialPoint`, else `mixed` (a grade-1 IPNS circle with einf≠0 stays undrawn). Order matters — `isVeronesePoint` runs first so the **origin** finite point (einf=lift=0, consistent) stays `finitePoint`. `renderPlanImpl` → `{kind:'specialPoint', x, y}` via `toEuclidean`. `KIND_COLOR.specialPoint='#1482C8'` (point-family blue). (b) `src/Canvas.jsx`: new `SvgPoint` `shape="hollow"` (stroked ring, `fill="none"` — a hollow dot ◎, user's pick) + a `specialPoint` render case mirroring `finitePoint` drag-eligibility, no radius circle. (c) `src/ExpressionPanel.jsx`: `KIND_LABELS.specialPoint='Special point'`, `getDisplayValue` → `Special point (x,y)`, added to `DRAWABLE_KINDS`. Verified headless: both user objects → `specialPoint` with correct coords; regressions intact (`point(2,1)`→finitePoint, `point(2,1,0.5)`→roundPoint, origin→finitePoint, vinf→infinityPoint, ellipse/circle→grade-7 conic, `!circle`→grade-1 einf≠0→mixed). Build clean; no new lint errors (pre-existing ref/unused-var errors unchanged).

133. **CCGA: point normalization by origin weight (P/−(P·e∞))** — "how should 0.2202e1 - 0.5598e2 - 0.7177eo1 - 0.7177eo2 be normalized? … the normalization formula should be P/-(P | einf)." That object is a special point (w=−0.7177, einf=0); the `norm` button used `A.Length` (GA magnitude → 1), which keeps the projective point but leaves eo weight ≠ 1 (and is degenerate for a *null* finite point, where Length≈0 → no-op). Changed `normalizeMVFinit` (`src/algebras/ccga/index.js`) to, for the point family (`finitePoint`/`roundPoint`/`specialPoint`), divide by the origin weight `w = einfWeight(P) = −(P·e∞)` so `P·e∞ = −1` (origin weight +1, position reads off e1/e2); guards `|w|<1e-10`. Everything else (conics, dipoles, gauge blades) still uses `A.Length`. Verified: special point → `eo − 0.3068 e1 + 0.78 e2`; finite point ×3 / round point ×−2 → weight 1 with correct coords; grade-7 ellipse → unchanged GA-magnitude path. Build + eslint clean.

134. **Hide `inorm` button for non-degenerate algebras** — "for non degenerate algebra: inorm is not needed remove it for all non degenerate algebra different than PGA." The ideal norm (‖A‖∞) only makes sense for a degenerate metric (r>0) where ideal objects live in the null subspace; PGA(2,0,1) is the only degenerate algebra (r=1), VGA/CCGA/CGA(3,1,0)/r010 all have r=0. In `src/ExpressionPanel.jsx`: added `isDegenerate = (algebra.info?.signature?.r ?? 0) > 0`; gated the `inorm` button render and its help-modal row on `isDegenerate`; and made `isIdealObj` require `isDegenerate` so non-degenerate "ideal" objects (VGA vectors, CCGA ideal/infinity points) now show the `norm` button instead of the (removed) `inorm` and don't auto-switch to it. PGA unchanged. Existing saved `normalizeMode:'inorm'` items in non-degenerate algebras stay correct since `normalizeMVIdeal === normalizeMVFinit` there. Build + eslint clean (no new errors).

135. **Also remove the `.inorm` postfix operator in non-degenerate algebras** — "also remove .inorm in non degenerated algebra." Follow-up to #134 (which hid the inorm *button*). In `src/graph/evalMVArith.js`: added `idealNormSupported = (algebra.info?.signature?.r ?? 0) > 0`, dropped `'inorm'` from `PROP_NAMES` unless supported (so the validator at the `.prop` check rejects it), and gated the evaluator branch `prop.val === 'inorm' && idealNormSupported` (else `.inorm` falls through to the blade-name parse → null → invalid expression). Help-modal expression-reference row now shows `A.inorm` / "ideal" only when `isDegenerate` (`src/ExpressionPanel.jsx`). Verified: CCGA `P.inorm` → invalid (doesn't parse), `P.norm` → ok; PGA both parse. Build + eslint clean.

136. **CCGA: "special ideal point" — pure-direction ideal vector (no radius)** — "[ideal point] 0.219e2 is kind of a 'special ideal point' and should not have an associated radius … use the same base point as the special point with an arrow." Parallel to #132: an *ideal* grade-1 vector (origin weight ≈ 0) with a linear e1/e2 direction but NO einf lift. The `idealPoint` render plan derives `rSq = (x²+y²) − 2(einf1+einf2)`; with einf=0 it wrongly gave `rSq = x²+y²` (spurious radius = |position|). Added a `specialIdealPoint` kind. (a) `src/algebras/ccga/index.js`: in `classifyImpl`'s ideal branch, after the `infinityPoint` test, if all einf coeffs (`v6−v3,v7−v4,v8−v5`) are ≈0 (rel. `max(|e1|,|e2|)·1e-5`) → `specialIdealPoint`, else `idealPoint`. `renderPlanImpl` → `{kind:'positionedVector', vx, vy, special:true}` with **no rSq** (so the Canvas positionedVector radius-circle branch, gated on `rSq!==undefined`, draws nothing). `KIND_COLOR.specialIdealPoint='#E8A000'`. (b) `src/Canvas.jsx`: in the `positionedVector` case, when `plan.special` also draw an `SvgPoint shape="hollow"` (the special point's hollow dot) at the arrow's base — so it reads as ◎—→. (c) `src/ExpressionPanel.jsx`: `KIND_LABELS.specialIdealPoint='Special ideal point'`, `getDisplayValue` → `Special ideal point (x,y)`, added to `DRAWABLE_KINDS` and `hasPosition`. (d) `src/useGraph.js`: added to `isVecVal` and `tipOf` (uses val[1]/val[2]) so its anchor positions like a normal ideal point. Verified: `0.219 e2`→specialIdealPoint, no rSq; `vector(1,2)`/`(1,2,0.5)`/`(0,0.219)`→idealPoint with rSq 0/0.25/0 (radius preserved); vinf→infinityPoint; finite/round/special points unchanged. Build clean; no new lint problems (17=17 pre-existing).

137. **CCGA: special ideal point draggable via its tail** — "the special vector should be draggable via its tail point." `hitTest` (`src/Canvas.jsx`) already has a value-driven anchor handler returning `dragType:'vector'` (→ `setDrawPos`) for vector-like values, but `specialIdealPoint` was missing from its `isVectorLikeVal` set and from the unlabeled-node guard. Added `valKind === 'specialIdealPoint'` to both (lines ~94 and ~188). Since `vectorPositions` already tracks it (#136 useGraph `isVecVal`), the hollow-base tail now drags the drawPos anchor (with nearby-anchor snapping) exactly like a normal ideal point — no other wiring needed. Build clean; no new Canvas lint (6=6).

138. **CCGA special ideal point: actually draggable (hitTest multivector-branch bug)** — "the vector cannot be drag." #137's hitTest additions were correct but never reached: `V = 0.219*e2` parses as a **`multivector`** node, and CCGA's `hasDepPointCoeffs` returns true for the `e2` coeff, so hitTest's `hasVariablePos` branch ran `toEuclidean` (null for an ideal point, origin weight 0) and `continue`d, skipping the node before the value-driven anchor handler. Fixed all three multivector sub-branches (`depPoint`/`dualDepPoint`/`litMVPoint`) in `src/Canvas.jsx` to wrap the position hit-test in `if (eu) {…}` instead of `if (!eu) continue;` — when `toEuclidean` is null (ideal/special-ideal value) it now falls through to the value-driven handler, which returns `dragType:'vector'` (tail → `setDrawPos`). Also makes plain `V = e1+2*e2`-style ideal points draggable by their anchor. Build + Canvas lint clean (6=6).

139. **CCGA special ideal point drag — verified correct; root cause was a crashed dev app (stale HMR)** — "again the special ideal point position cannot be drag." Traced the entire path end-to-end with the real pipeline: `V = 0.219*e2` → `spec.evaluate` → `classifyMV` = `specialIdealPoint` → `getRenderPlan` = `{positionedVector, special:true}` → eligible for `vectorPositions` (useGraph `isVecVal`) → a faithful replica of `hitTest` returns `{id, dragType:'vector'}` at the origin anchor → `handlePointerMove` `dragType==='vector'` → `setDrawPos` → `SET_DRAW_POS` reducer updates `item.drawPos`. (`hasVariablePos` is false for the literal `0.219`, so #138's multivector-branch edit was a no-op for this case; the enabling fix was #137 adding `specialIdealPoint` to the value-driven anchor handler + `isVecVal`.) The dev-server log showed the real cause: a recurring `TypeError: (intermediate value)() is null` in `<AppShell>` (at a now-deleted `App.jsx:508`, i.e. a **stale HMR bundle**) — a crashed React tree makes the whole canvas non-interactive, so nothing dragged regardless of correct code, and HMR never recovered. Killed the stale vite and started a fresh tracked server (HTTP 200, clean log). Resolution: hard-reload the page. No code change this entry.

140. **CCGA special ideal point: arrowhead drags the direction** — "in CCGA if I define `e2` it's a Special Ideal Point but I can neither drag the position of the vector or the vector arrow head." Root cause found via a full app-state hitTest simulation: the **tail** anchor (at the origin) already returned `dragType:'vector'` (reposition works), but the **tip/arrowhead returned null** — there was no tip handler, and an invisible origin anchor is not what a user reaches for. Added arrowhead dragging that rewrites the literal direction. (a) `src/useGraph.js`: new `updateSpecialIdealPoint(nodeId, vx, vy)` — for a literal `multivector` node, rewrites the text to `vx*e1 + vy*e2` (stays a special ideal point: only e1/e2, no einf), preserving the label; exported it. (b) `src/Canvas.jsx`: hitTest value-driven handler now checks the tip first for a `specialIdealPoint` literal multivector → `dragType:'specialIdealTip'` (tail still → `'vector'`); `handlePointerMove` maps `specialIdealTip` → `updateSpecialIdealPoint(id, x-pos.x, y-pos.y)`; added it to the `useGraphContext` destructure and `snap.current`; `tipDraggable` and `isTipDrag` now include the special-ideal case so the arrowhead shows the grab handle/hover. Derived (mvExpr) special ideal points stay tail-only (can't rewrite a computed value). Verified: tail→`vector`, tip→`specialIdealTip`, and the `e2`→`1.4*e1 + 2*e2` rewrite re-classifies as `specialIdealPoint`. Build + lint clean (13=13).

141. **CCGA: imaginary circles & ellipses now render (dashed)** — "add imaginary circle and imaginary ellipse in dashed drawing — e.g. `imCircle = !(point(-0.75, 1.24) + 2einf)`." An imaginary conic (real centre, no real point locus — here −½r²=+2 ⇒ r²=−4) was silently dropped: `conicGeometry` returned `{subtype:'empty'}` for the `disc<0` case when `rx2=−Fp/Ap` and `ry2=−Fp/Bp` came out negative, and `getRenderPlan` then returned `null`. Since the imaginary branch only ever arises in the ellipse/circle-type case, the magnitudes `√|rx2|`,`√|ry2|` are exactly the semi-axes to draw. (a) `src/algebras/ccga/index.js`: in `conicGeometry`'s `disc<0` branch, hoisted the `circle` (Ap≈Bp) test above the negativity check and replaced the `'empty'` return with a real ellipse/circle geometry carrying `imaginary:true` (axes via `Math.sqrt(Math.abs(...))`). The `imaginary` flag rides through `getRenderPlan`'s `{kind:'conic', ...geom}` spread automatically; the defensive `subtype==='empty'` guard stays (now unreachable for these). (b) `src/Canvas.jsx`: the `conic` dispatch now defaults imaginary conics to `'dashed'` (taking precedence over the CGA-derived `'dotted'`): `strokeStyle ?? (plan.imaginary ? 'dashed' : plan.cga ? 'dotted' : null)`. `SvgConic`'s ellipse/circle branch already routes `strokeStyle` through `resolveStrokeDash`, so no change there; an explicit per-item stroke style still wins. Hyperbolas/parabolas can't be imaginary, so they're untouched. `cga310` already drew imaginary circles dashed (via the `circle` kind) — no change needed. No new lint errors (6=6 pre-existing).

142. **CCGA: complete object-zoo classification (multipole ladder, conic pencils, flats)** — "E^F should be a Twopole not a point pair; same E^F^G→Tripole, …; how to call E^F^Iod, E^F^Iinfd, etc.; propose a complete plan to classify and manage all useful objects." Extended `classifyImpl` (`src/algebras/ccga/index.js`) to cover the whole paper master table, driven by gauge-factor detection (a blade `G` divides `val` iff `‖val∧G‖/‖val‖≈0`, the existing per-grade `w(gauge)` helper). New kinds: `twopole` (gr2, was mislabeled `pointPair`), `tripole` (gr3), `conicPencil` (Iod-gauged partial conic, carries `n`), `lineAtInfinity` (pure-einf gr3 = Iinf), `conicAtInfinity` (Iod∧Iinf gr5), `pseudoscalar` (gr8); `quadpole` (gr4) already existed. Per-grade: gr2→twopole; gr3→ round point (Iinfd) / line@inf (pure einf) / conic pencil n=1 (Iod) / tripole; gr4→ flat point / point pair (Iinfd) / conic pencil n=2 (Iod) / quadpole; gr5 (rewritten); gr6→ conic pencil (Iod, n=2 if also Iinfd else 4); gr7→ conic (unchanged — both-gauge `p1p2p3∧Iinfd∧Iod` reads off as the same circle/line); gr8→ pseudoscalar. **Key finding:** all CCGA points live in the 6-D subspace V₆={e1,e2,eo,einf1,einf2,einf3}, so at gr5 the `∧Iinfd` test always vanishes (5+2 overflows 6-D) and cannot distinguish a CGA circle from a bare pentapole — but `Iod` is transverse, so `w(Iod)≈0` is the real discriminator. Consequently 5 points uniquely determine a conic (the bare "pentapole" IS that conic and renders with subtype from `conicGeometry`); only the tripole (2-param net) and quadpole (1-param pencil) are genuinely locus-free. `classifyGrade5` rewritten to route Iod-gauged objects (conic@inf / 3-pt pencil) first, everything else → conic. **Rendering (per user):** abstract objects (twopole/tripole/quadpole, conic pencils, conic@inf, line@inf, pseudoscalar) are classify+label-only — no `getRenderPlan` case → `null`, not in `DRAWABLE_KINDS`; the bare twopole `E^F` no longer draws (the drawable dipole is the CGA `E^F^Iinfd`). Wiring: `KIND_COLOR` (purple multipoles, light-red `conicPencil`, orange ideals, gray pseudoscalar); `INITIAL_ITEMS` showcase `pp = P1^P2` → `P1^P2^Iinfd` so the default still draws a (correctly-named) CGA point pair; `src/ExpressionPanel.jsx` `KIND_LABELS`+`getDisplayValue` cases (`conicPencil` → "Conic pencil (n pts)"), and a new **Object zoo (CCGA)** help-modal table (gated on `algebra.mvConsts?.Iod`). Verified headless across the full zoo (twopole/tripole/quadpole→null; pentapole/CGA-circle/CGA-pair→drawn; pencils/flats/pseudoscalar→labeled-not-drawn). Build ✓; ccga lint clean; ExpressionPanel 6=6 pre-existing ref errors.

143. **Auto-named clicked points must skip reserved constants (CCGA `I`)** — "when we create points by clicking it creates E,F,G,H,I,J… — `I` overlaps the pseudoscalar; what do you propose instead?" Double-click creates a free point auto-named from `AUTO_POINT_NAMES='EFGHIJKLMNOPQSUVWYZ'`; the 5th, `I`, is CCGA's pseudoscalar constant (in `algebra.mvConsts`), so `I = point(...)` shadowed the built-in and broke `Iod^Iinf`, the Object-Zoo `I` ref, etc. Fix (`src/useGraph.js`): `pickPointName(usedIds, reserved)` now also skips names in a `reserved` set (and the `P{i}` fallback too); `addFreePoint` builds `reserved = new Set(Object.keys(algebra.mvConsts ?? {}))` and passes it in. Kept the letter scheme; only collisions are skipped, per active algebra. Result — CCGA: E F G H **J** K L M… (I skipped); cga310/PGA/VGA unchanged (cga310's mvConsts has no single-letter key; PGA/VGA expose none, so `I` stays). Multi-char keys (`Iod`,`Iinf`,…) are harmless to include since auto-names are single letters. Verified by simulation (CCGA "E F G H J K L M", no-mvConsts "E F G H I J K L"); no new lint errors.

144. **CCGA: both-gauge objects are the CGA family, not conic pencils** — "E^F^Iinfd^Iod is classified as Conic pencil (2 pts) [gr6]." Wrong: like `E^F^G^Iinfd^Iod` (gr7) is the **Circle** from CGA, `E^F^Iinfd^Iod` (gr6) is the **Point pair**, and `E^Iinfd^Iod` (gr5) the **Round point** — the CGA family re-expressed in the conic (Iod) frame. Bug: the gr5/gr6 branches of `classifyImpl` (`src/algebras/ccga/index.js`) returned `conicPencil` whenever `Iod` was a factor (`w(Iod)≈0`) without checking `Iinfd`. Discriminator (empirical): a genuine Iod-only pencil has `w(Iinfd)≠0` (~0.4–0.5), a both-gauge CGA object has `w(Iinfd)≈0` (Iinfd is also a factor). Fix: when `w(Iod)<1e-6` (and not the pure-einf conic-at-infinity), if `w(Iinfd)<1e-6` too → `classifyImpl(A.LDot(Iinfd, val))` — contracting with Iinfd strips Iod and drops the object to the standard Iinfd-gauged CGA form (gr5→gr3 round point, gr6→gr4 point pair). Those recovered classifications already carry `ccgaPoint`/`ccgaPair`, so they render with **zero extra plumbing** (no render-plan/color/label changes). Recursion terminates (recovered object is lower grade, no Iod factor). Verified headless: `E^Iinfd^Iod`→finitePoint(drawn), `Er^Iinfd^Iod`→roundPoint(drawn), `E^F^Iinfd^Iod`→pointPair(drawn) — each mirrors its `^Iinfd` form one grade down; regressions intact (`E^F^Iod`/`E^F^G^Iod`/`E^F^G^H^Iod`→conicPencil 2/3/4, `Iod^Iinf`→conicAtInfinity, full 5-pt→conic). eslint clean.

## 2026-06-16

145. **CCGA: render the n-pole ladder as its points + a dashed outline** — "for n-pole link the n point together with a dotted trait (just like conic of grade 5)." The multipoles twopole `p1∧p2` (gr2), tripole `p1∧p2∧p3` (gr3), quadpole `p1∧p2∧p3∧p4` (gr4) were classified+labeled but **not drawn** (#142). Now each renders as its n defining points (dots) joined by a dashed polygon — the dotted-trait style the all-point list already uses (`SvgPolygon`, dash `4 3`). **Extraction without porting `extract.py`'s radicals** (SVD+Cardano+Ferrari): the n points lie on a GA-native carrier conic read straight off the blade, so I scan the membership residual `‖point2D(x,y) ∧ Npole‖` along it for the n zeros — exact enough for rendering and reusing the existing `conicCoeffs`/`conicGeometry` pipeline. New helpers in `src/algebras/ccga/index.js`: `rawNorm` (L2 with no `objectWeight` `|1` floor), `sampleConicWorld(geom,n)` (world-space parametric samples mirroring `SvgConic` — circle/ellipse/hyperbola/parabola/line/linePair), `refineChord` (golden-section refine along the local chord), `findMembershipZeros` (lowest-n local minima, deduped, with a **degeneracy guard**: bail if `max(residual) < 1e-6·‖Npole‖`, i.e. a flat-zero carrier — see below), `extractTripole` (carrier `T∧Iod∧Iinfd`), `extractQuadpole` (carrier = a non-degenerate pencil member `(Iod∧Q∧p5)·Iinv`, trying several `p5`). twopole reuses the closed-form `extractDipole`. Three `renderPlanImpl` cases → one new `multipole` kind `{points, imaginary}`. **Key finding:** a *collinear* tripole is genuinely unrecoverable — its lifted points are linearly dependent, so the carrier degenerates to a line and `q∧T=0` holds along the **whole** line (residual ≡0, verified), not at 3 isolated points (the Python `extract.py` also raises here); the degeneracy guard catches it (`maxRes≈4e-14 ≪ 1e-6·‖T‖`) → returns null (labeled, not drawn). First guard attempt (`minRes>0.5·maxRes` contrast) failed because Float64 noise left `maxRes≈4e-14>0` while `minRes=0` — needed an absolute floor tied to `‖Npole‖`. `src/Canvas.jsx`: `SvgMultipole` (dots filled/hollow by reality + dashed polygon, vertices angle-sorted around the centroid so the quad is non-crossing) + a `multipole` dispatch case. `src/ExpressionPanel.jsx`: added `twopole`/`tripole`/`quadpole` to `DRAWABLE_KINDS` and rewrote the Object-zoo help rows ("drawn as their defining points joined by a dashed outline"). Showcase: added `T = P1^P2^P3`. Drawing the bare twopole intentionally reverses #142's "bare twopole not drawn" (it coincides with the CGA dipole `E^F^Iinfd` — same two points). Verified headless through the real `classifyMV`/`getRenderPlan`: twopole/tripole/quadpole recover their points (<1e-2 after refine), collinear tripole → null. Build ✓; ccga lint clean.

## 2026-06-17

147. **CCGA scaling versors (dilators) + generic `exp`/`>>>`** — "check Workspace/notebook/ccga to modelize versor of scaling (isotropic and anisotropic) … exp of +1 square blade … exp(eo^einf) should give a versor of scaling." Researched `notebook/ccga` (`transform.py::dilator`, cheat sheet §6): each null pair gives a hyperbolic bivector `(eoᵢ∧einfᵢ)²=+1`; isotropic dilation `D(s)=∏ᵢ(cosh u+sinh u·(eoᵢ∧einfᵢ))`, `u=½ln s`, applied by sandwich, scales `point(x,y)→(s·x, s·y)`. **Key finding (verified in throwaway JS against the real CCGA module): ganja's analytic `.Exp()` is WRONG here** — it gives (5.01,7.51) not (4,6) for s=2 because the combined dilation bivector `eo1∧einf1+eo2∧einf2+eo3∧einf3` is *not simple*. Used instead (both exact to (4,6)) the closed-form product for `dilator(s)` and a **scaling-and-squaring Taylor `exp`** (`ccgaExp`, all products via `A.Mul`) wired as the spec's `expFn` (overrides the default `mv.Exp()`). Anisotropic was **deferred per the user** — a single pair `eo1∧einf1` does *not* isolate an axis (gives ~4/3 uniform) because CCGA's Veronese embedding couples the conformal pairs to Euclidean coords indirectly; there's no `dilator_xy` in the notebook. Shipped the toolkit instead: enabled `'motorExp'`+`'motorApply'` in CCGA `SUPPORTED_NODE_TYPES`, added the `dilator(s)` named constructor + the `Edil = eo1^einf1+eo2^einf2+eo3^einf3` isotropic-generator blade to `mvConsts`, registered `'dilator'` in parseExpression `BUILTIN_CONSTRUCTOR_NAMES`, added `log`/`ln` scalar builtins (`Math.log`) for the `exp(½·log(s)·Edil)` route, and relaxed evalMVArith's constructor arg-count gate from `≥2` to `≥1` (single-arg versor constructors). Help reference (`ExpressionPanel.jsx`) got `dilator`/`exp`/`>>>` rows; default `INITIAL_ITEMS` left untouched per the user. A versor (even-grade MV) classifies as `mixed` → not drawn (expected); the *transformed* object renders. Verified end-to-end through the real parser+nodeTypes: `D=dilator(2)`, `D>>>P1`→(4,6); `exp(0.5*log(2)*Edil)>>>P1`→(4,6); `dilator(2)>>>circle(0,0,1)`→circle r=2; `dilator(-1)`→null. Build clean; no new lint in touched files.

146. **CCGA n-pole rendering lag → closed-form GA point extraction** — "n-pole rendering is very long and so it makes the app laggy" + "to get the intersection point check Workspace/ccga or the saved graphs." The #145 membership-scan fired 200–240 ganja `Wedge` per drag tick (~19 ms). Replaced it with the closed-form, GA-native recovery the user had already validated in the CCGA saved graphs — no scanning, no SVD (all in `src/algebras/ccga/index.js`): **twopole** keeps `extractDipole` (closed form). **quadpole** = Ferrari from `saved_graphs/ccga_extract_lines.json`: build two independent pencil members `(Iod∧Q∧p5)·Iinv`, form the degenerate-pencil cubic `det3(co1+t·co2)=0` from 4 `det3` evals (the `½(g(1)±g(-1))` trick), solve via Cardano (`solveCubicReal`, trig form), `factorLinePair` the degenerate member → two lines, and meet each with a pencil conic via the line∩conic quadratic from `ccga_line_conic_intersection.json` (`lineConicPoints`) → 4 points. **tripole** (3 points = a 2-param net, no clean pencil) → circumcircle `T∧Iod∧Iinfd` + a precomputed **quadratic-form residual**: `wedgeResidualForm(B)` builds the 8×8 `M[i][j]=(e_i∧B)·(e_j∧B)` from 8 wedges once, then each `‖q∧T‖²` is a pure-arithmetic `qᵀMq` over point2D's 8 coords (no ganja/alloc per sample); `findMembershipZeros`/`refineChord` now take a residual fn. **Key bottleneck fix:** the OPNS→IPNS dual `Mul(C7,Iinv)` is a dense 256-dim product (~2.5 ms); since a grade-7 blade dualizes *purely* to grade-1, precomputed an 8×8 `DUAL7` map (`dualGrade7Coeffs`, built from 8 Muls at load) and routed `conicCoeffs` (grade-7 path) and `pencilMember` through it — also speeds up **all** conic rendering. Refactored the inline `delta3` in `conicGeometry` into the shared `det3(co)`. Removed the dead `sampleConicWorld`/`membershipResidual` scan path. **Perf (fresh-value getRenderPlan, full recompute):** quadpole 19→**2.2 ms**, tripole →**3.3 ms**. Verified headless: bare twopole/tripole/quadpole recover their defining points (<5e-3); a conic∩conic quadpole built the app way (`C1 & C2 | Iinfd`, `|`=Dot) recovers the 4 shared points; collinear tripole → null; 5-pt conic render unchanged (fast dual is exact). Build + ccga lint clean. Rendering (SvgMultipole, render-plan cases, showcase, help) untouched.

## 2026-06-18

148. **CCGA: render pairs of ideal directions + adapter gap list** — "check Workspace/ga-constructor and the ccga adapter, list all the things that should be added; currently pair of ideal direction are not rendered." Reproduced: `vinf(1,0)^vinf(0,1)` classifies `twopole` but `getRenderPlan` → null. Root cause: a pair of points *at infinity* is a grade-2 blade living entirely in the 3-D `einf` space, so `extractDipole`'s `m=−(einf⌋pp)` collapses (`mSq≈0`) and it bails. **Fix** (`src/algebras/ccga/index.js`): new `extractIdealPair(B)` reads the three `einfᵢ∧einfⱼ` coefficients by contracting with the reciprocal `eoᵢ∧eoⱼ` (`⟨(eoᵢ∧eoⱼ)·B⟩₀ = −Bᵢⱼ`, since `eoᵢ·einfⱼ=−δᵢⱼ`; signs verified empirically) and solves the homogeneous quadratic `½B₂₃·vx² + B₁₂·vx·vy − ½B₁₃·vy² = 0` (a direction lies in span(v1,v2) iff `w∧B=0` and `(a,b,c)=(½vx²,½vy²,vx·vy)` on the conic at infinity): 2 real roots → secant pair (hyperbola-type), 1 double → tangent (parabola-type), disc<0 → imaginary pair (ellipse-type, no real direction). The `'twopole'` render case now falls back to `extractIdealPair` → new plan `{kind:'idealPair', dirs, imaginary}`. **Canvas** (`src/Canvas.jsx`): new `case 'idealPair'` draws one `SvgVector` arrow from the origin per direction + an `SvgIdealPointMarker` on the boundary ellipse (mirrors the single `infinityPoint` arrow); imaginary pair draws nothing solid. Verified headless through the real `getRenderPlan`: `vinf(1,0)^vinf(0,1)`→(1,0),(0,1); `vinf(2,1)^vinf(-1,3)`→both source directions recovered (up to sign); `(4·einf1−einf2)^einf3`→imaginary. Build ✓; no new lint in touched files. **Gap list** (the "list all things to add" half — recorded in the plan file, not yet built): (A) classified-but-undrawn objects `conicPencil` n1–4, `lineAtInfinity`, `conicAtInfinity`; (B) missing named constructors — degenerate conics (`line_pair`/`parallel_line_pair`/`secant_line_pair`), 3-point named conics, `tangent_at_infinity`, CGA `Iinfd` round-family wrappers, polars/tangents/normals/Apollonius, 5-tangent conic; (C) meet completeness — genuine 4-point conic∨conic (grade-6 `conicPencil n4`) should wire the existing `extractQuadpole`; (D) read-off properties (center/axes/eccentricity/foci/discriminants) not surfaced in the panel.

149. **CCGA: drop `vinf`, keep only `vector` (Veronese point at infinity)** — "remove vinf constructor to only keep vector; currently the vector tail and head cannot be moved freely by user drag." CCGA had two names for the same Veronese point-at-infinity primitive (`vinf` and a `vector` alias, both → `freeInfinityPoint`/`infinityPoint2D`); confirmed via parse/classify probe that `vector(2,1)` already builds the `infinityPoint` (`dir` preserved) and that Canvas already wires both the tail (`dragType:'vector'`→`setDrawPos`) and tip (`freeInfinityPointTip`→`updateFreeInfinityPoint`) drag paths. Per the user's choice (keep Veronese math, just rename), dropped the `vinf` name everywhere so `vector(...)` is the sole constructor and dragging rewrites `vector(...)` consistently: `parseExpression.js` removed `vinf` from `BUILTIN_CONSTRUCTOR_NAMES` and the `freeInfinityPoint` branch's `vinf` aliases (only `vector` now); `evalMVArith.js` inline constructor `vinf`→`vector` (overrides the `vector2D` registration so inline `vector()` matches the free node = `infinityPoint2D`); `useGraph.js` `updateFreeInfinityPoint` tip-drag now emits `vector(...)`; help table (`ExpressionPanel.jsx`) collapsed the two rows into one `vector(vx,vy)` = "Point at infinity (CCGA Veronese limit), draggable tail & tip"; comment cleanups in `nodeTypes.js`/`Canvas.jsx`. Verified headless: `vector(2,1)`→`freeInfinityPoint`, classifies `infinityPoint`, renderPlan `positionedVector {vx:2,vy:1}`; `vinf(2,1)` no longer a constructor (→ `mvExpr`). No new lint in touched files (pre-existing `ExpressionPanel.jsx` errors only).

150. **CCGA `vector(x,y)` → pure-linear direction (drop Veronese embedding)** — "Again the drag of the arrow is laggy. The problem is that the arrow is drawn from its square coordinate. vector(x,y) should draw the real vector (x,y) not (x²,y²)." The Veronese point-at-infinity (#149) *inherently* stores `½x²·einf1 + ½y²·einf2 + xy·einf3` — so the value's coefficients (and the panel, and every dependent recompute) carry the squares, and direction sign can't even be represented (`infinityDir` forces vx≥0). Reversing the #149 "keep Veronese" choice per the new instruction: `vector(x,y)` now builds the **bare Euclidean direction `x·e1 + y·e2`** (no eo/einf lift). Changes: `vector2D` (`src/algebras/ccga/index.js`) rewritten to set only `v[1]=x, v[2]=y` (dropped the quadratic terms and the `r` circle); CCGA `SUPPORTED_NODE_TYPES` swapped `freeInfinityPoint`→`freeVector` (reusing the already-wired `freeVectorTip`+`updateFreeVector` tip drag and `dragType:'vector'` tail drag, both rewriting `vector(...)`); reverted the #149 evalMVArith override so inline `vector` = `vector2D` again; help row updated to "Free direction vector `vx·e1 + vy·e2`". Result: `vector(2,1)` parses `freeVector`, coeffs literally `[2,1,0,0,0,0,0,0]`, classifies `specialIdealPoint`, renderPlan `positionedVector {vx:2,vy:1,special:true}`; `vector(-3,2)` → `{vx:-3,vy:2}` (signed, unlike Veronese). Cheap value ⇒ no per-tick Cardano/sqrt, fixing the drag lag. `infinityPoint2D` kept (exported, derived/idealPair use) but has no constructor name now. eslint clean on the 3 core files (pre-existing `ExpressionPanel.jsx` ref-in-render errors only); `npm run build` ✓.

151. **Drag freeze for label-less free objects: node id drifts with the text** — "when I use vector(1,1) and then I drag its head it transformed it into a vinf and it slightly move the head but freeze instantly." Root cause (general, not vector-specific): `parseExpression` derives a label-less node's id from its text — `const id = label ?? ('_' + expr.replace(/\s+/g,''))` — so `vector(1,1)`→`_vector(1,1)`, but the first drag tick rewrites the text to `vector(1.05, 1.02)`→id `_vector(1.05,1.02)`. The drag captured the *old* id at pointer-down (`ptDragRef.current.id`), and every `updateFree*`/`updateVector` finds its item via `items.find(it => parseExpression(it.text)?.id === nodeId)` — which no longer matches after tick 1, so the drag freezes (the "transformed into a vinf" half was the stale pre-#150 build, where `vector`→`freeInfinityPoint` and the old `updateFreeInfinityPoint` rewrote `vinf(...)` on tick 1 before freezing). Affects *all* label-less free draggables (point/flatPoint/vector); labeled ones are stable (id = label). **Fix** (`src/Canvas.jsx`): added `items` + `parseExpression` to the `snap` ref; at pointer-down capture the stable React `item.id` (`ptDragRef.current.itemId`); in `handlePointerMove`, re-resolve the *live* node id from that item.id each tick (`id = parseExpression(dragItem.text)?.id`) before dispatching — so the update always targets the current node. General fix (also covers depPoint/litMVPoint text-rewrite drags). Verified by simulating the drag loop against the real `parseExpression`: old (captured-id) approach `froze` from tick 2, new (item.id re-resolution) works across all ticks. eslint: only pre-existing unused-var errors in Canvas.jsx; `npm run build` ✓.

152. **Cleanup: remove orphaned `freeInfinityPoint` node plumbing** — "continue." After #150 switched CCGA's free `vector` to the `freeVector` node type, the `freeInfinityPoint` node type had no remaining producer (CCGA was its only consumer via `supportedNodeTypes`). Removed the dead plumbing across four files: the `freeInfinityPoint` parse branch (`parseExpression.js`), the `freeInfinityPoint` node-type block + its `infinityPt2D` const (`nodeTypes.js`), `updateFreeInfinityPoint` + its export (`useGraph.js`), and the Canvas hit-test block, `freeInfinityPointTip` drag branch, `snap`/destructure wiring, and the `isTipDrag`/`tipDraggable` mentions (`Canvas.jsx`). Kept `infinityPoint2D` (the function + spec export) — it documents the Veronese embedding and the `infinityPoint`/`idealPair` classify+render paths still handle *derived* points-at-infinity (built via blade arithmetic), which don't go through the constructor. Verified: `grep` shows zero remaining `freeInfinityPoint`/`updateFreeInfinityPoint`/`infinityPt2D` refs; `vector(2,1)`→`freeVector`→`specialIdealPoint`→`positionedVector {vx:2,vy:1}` unchanged; `npm run build` ✓; lint shows only pre-existing errors.

153. **`vector(x,y)` restored to the Veronese ideal point (value = squares, arrow = (x,y))** — "the multivector associated to vector(1,1) should be 0.5·x²·einf1 + 0.5·y²·einf2 + x·y·einf3 the natural ideal point in CCGA." This reconciled the whole thread: the #149 "draws (x²,y²)" complaint was really the #151 drag-*freeze* (label-less id drift) misattributed — with that fixed, the **value** should be the Veronese embedding (the natural CCGA ideal point) while the **arrow** still draws at the real (x,y) via the stashed `.dir`. Reverted #150's pure-linear `vector2D`: it now delegates to `infinityPoint2D` (single source of truth, `export function vector2D(x,y){ return infinityPoint2D(x,y); }` — hoisted, so order-independent). Kept the `freeVector` node type (its `positionedVector` render/drag path handles the `infinityPoint` classification fine) and the #151 id-drift drag fix, so dragging is smooth and stays `vector(...)`. Updated the help row (`½vx²·einf1 + …`) and the evalMVArith comment. Verified via the null-basis display: `vector(1,1)`=`0.5·einf1 + 0.5·einf2 + 1·einf3`, `vector(2,3)`=`2·einf1 + 4.5·einf2 + 6·einf3` (= the formula), classifies `infinityPoint`, renders signed arrows toward `(x,y)` incl. `vector(-3,2)`→`{vx:-3,vy:2}`. eslint clean on core files; `npm run build` ✓.

154. **Vector tail/anchor + position attribute don't move it — `setDrawPos` writer dropped `infinityPoint`** — "the vector cannot be moved using its anchor position (tail) also modification of its position attribute do not move it." Root cause: every *reader* of anchorability already accepted `infinityPoint` (the `vectorPositions` `isVecVal` test, the canvas `freeVector`/value-driven hit-tests offering the `dragType:'vector'` tail handle, and the ExpressionPanel `hasPosition` position sub-row), but the *writer* `findVectorItem` (gating `setDrawPos`/`setDrawPosRef`) did **not** — its kind list was `idealPoint|vector|idealFlatPoint|bivector`. So both the tail drag and the panel's position-attribute edit (`ExpressionPanel.jsx:541` → `setDrawPos`) silently no-op'd for the Veronese vector. **Fix** (`src/useGraph.js`): added `specialIdealPoint` + `infinityPoint` to `findVectorItem` (now in sync with `vectorPositions`, noted with a comment), and added an `infinityPoint` case to `tipOf` (`tail + val.dir`) so a linked anchor pinned to an infinity-vector's tip resolves to head, not origin. Verified: `vector(1,1)`→`infinityPoint`, `findVectorItem` predicate now `true` (setDrawPos fires), `tipOf` reads `dir={1,1}`. eslint: only pre-existing useGraph errors; `npm run build` ✓.

155. **Simplify n-pole rendering: dashed segment between every pair of points** — "for the n pole rendering simplify it by just draw a dashed trait between all the points of the n-pole (segment for two point, triangle for three, quad with diagonal for four)." `SvgMultipole` (`src/Canvas.jsx`) previously drew a dashed *polygon outline* — angle-sorted points joined by a closed `polyline`, so a quadpole showed a 4-edge quad with no diagonals. Rewrote it to connect every pair of points (complete graph) with individual dashed `<line>`s: K2 = one segment, K3 = triangle, K4 = quad + both diagonals. Dropped the centroid angle-sort and the polyline/close logic (simpler); kept the centroid only for label placement and the filled/hollow (imaginary) point dots. Build ✓; no lint in the touched region.

156. **CCGA rotor: fix incorrect three-exp form + the lag it caused** — "check Workspace/ga-constructor saved graph ccga_rotor — why is it so laggy? is the exp of B2 and B3 well defined? is it the correct form of the rotor in CCGA?" Diagnosed all three numerically against the real adapter. **(1) Lag:** `ccgaExp` (`src/algebras/ccga/index.js`) is scaling-and-squaring over a fixed 18-term Taylor series, each term a ~3.5 ms 256-dim `A.Mul`; the graph ran *three* exps (`R1=exp(0.5·angle·e12)`, `R2=exp(angle·B2)`, `R3=exp(angle·B3)`) + product + `A.sw` sandwich on every `angle` change ⇒ ~250 ms/tick. **(2) exp well-defined?** Yes but trivially: `B2=eo3∧einfb` and `B3=einf3∧eob` are wedges of two *orthogonal null* vectors, so `B2²=B3²=0` (nilpotent, verified) — `exp(t·B)=1+t·B` exactly, a transvection, NOT a rotation generator (which must square negative). **(3) Correct form?** No. The product `R1*(R2*R3)` rotates a point's *position* (x,y) rigidly about the origin but *destroys the Veronese lift* (`veroneseErr≈1–15`), so the result classifies `mixed` — not a valid CCGA point/conic. Root cause: multiplying *separate* exponentials of non-commuting nilpotents. Their **sum** is a genuine rotation generator: `(B2+B3)²=−2` (+ grade-4). **Fix:** a single combined exponential preserves the variety; solved the coefficients → `R = exp(-0.5*angle*e12 - angle*(B2+B3))` rotates points by `+angle` about the origin AND keeps `veroneseErr≈1e-12` (validated on 4 points; the quadratic einf-plane must rotate at `2θ`, hence the `-angle` vs `-0.5·angle` factor-of-2). Rewrote `saved_graphs/ccga_rotor.json` to the single-exp form (6 items: B2, B3, angle, R, E, R>>>E) with a regenerated matching `hash`. Also added an early-exit to `ccgaExp`'s Taylor loop (bail when a term's L1 norm < 1e-16) — a rotor now uses ~11 Taylor muls instead of 18, and the chain drops from 3 exps to 1 (~4.5× fewer dense products/recompute). Verified end-to-end: `R>>>(-13,22)=(-21.96,13.07)` = true rot(+0.5), veErr≤1.5e-12 ⇒ classifies `finitePoint`.

## 2026-06-23

157. **Rework the CCGA adapter for performance (sparse product engine) + modular split** — "I would like to rework the ccga-adapter with performance concern." Decided (with the user): scope = performance **+** refactor; engine = **replace** ganja's dense products with a sparse engine; driver = heavy graphs / general preventive speed. **Root cause:** in ℝ(5,3) every GA op routed through ganja's *dense* 256-dim products (`Mul`/`Wedge`/`Dot`/`LDot`/`Vee`/`sw`/`Dual`/`Reverse`/`Length`, ~2–3.5 ms each) — across the shared `evalMVArith`/`nodeTypes` and pervasively inside the adapter (`ccgaExp`, `extract*`, `classifyGrade*`, `dilator`) — even though a CCGA point has only 8 of 256 non-zero coeffs. **Phase 1 — sparse engine** (`src/algebras/ccga/product.js`, new): precomputes the Cayley table *analytically from bitmasks* at load (instant; ganja's own 256² sweep is ~140 s) — `MUL_K/MUL_S` (reorder-parity × metric, e6–e8 square −1) plus grade-selected `WEDGE_S/LDOT_S/DOT_S`, `REV_S`/`CONJ_S`, and `DUAL_K/DUAL_S` (= right-mul by the unit pseudoscalar, matching ganja's `Dual`). Kernels iterate operand *support* only and allocate results as `new A()` so ganja's `.Exp/.Log/.Inverse` getters still resolve. Conventions read straight from ganja source: `sw=a·b·~a`, `Length=√|⟨a·Conj(a)⟩₀|`, `Vee`=dual-of-wedge-of-duals. Wired in `algebra.js` by overriding the CCGA instance's static methods right after construction, so the *entire* compute path (load consts, classify gauges, exp/extract, shared evaluator/nodeTypes) speeds up with **zero changes to shared graph code**. **Verification** (`scripts/ccga_verify.mjs`, new): Part A compares every kernel to a pristine ganja Algebra(5,3) over all 256×256 basis pairs + unary blades + random dense MVs → **0 mismatches** (numerically identical); Part B replays all 41 `saved_graphs/ccga_*.json` → 0 integration failures, all render plans finite. Also diffed render-plan snapshots (`scripts/ccga_snapshot.mjs`, new) of the committed pre-change state vs the reworked tree → **byte-identical across all 40 common graphs**. Measured speedup: sparse wedge ~126× (208→1.65 µs/op), sparse mul ~1850× (2155→1.16 µs/op). **Phase 2 — modular split:** the 1376-line `index.js` split into `product.js` / `algebra.js` (instance, basis, engine wiring, null basis) / `embed.js` (Veronese map, conic constructors, Euclidean readers) / `conic.js` (coeff extraction, geometry, solvers) / `extract.js` (dipole/n-pole/ideal-pair) / `classify.js` (classifier, render-plan, norms) / `display.js` (null-basis display) / `index.js` (versors, colors, showcase, spec assembly), acyclic imports, identical public `spec`; snapshot re-confirmed byte-identical. eslint clean on all new files; `npm run build` ✓.

## 2026-06-25

158. **Add AACCGA — Axis-Aligned Conic Conformal GA ℝ(4,2)** — "I would like to add a new algebra R(4,2) which is the 'axis align conic algebra' with eo, einf, eob, einfb, Ie same as R(5,3) … point(x,y)=eo+e1·x+e2·y+0.5·einf1·x²+0.5·einf2·y² … C = p1^p2^p3^p4^eob (grade 5, n−1). create a new feat branch." Built `feat/aaccga`. AACCGA is the simpler sibling of CCGA: 6 generators / 64 blades, **two** null pairs (no cross-term `einf3`/`xy`), so it models *axis-aligned* conics `Ax²+By²+Dx+Ey+F=0` (C≡0). ganja `Algebra({p:4,q:2})` → e1,e2 Euclidean; e3,e4 = e₊ pair; e5,e6 = e₋ pair. Null basis `eo1=e3+e5, eo2=e4+e6, einf1=(e5−e3)/2, einf2=(e6−e4)/2`; `eo/einf/eob/einfb`, grade-2 `Io=eo1^eo2`, `Iinf=einf1^einf2`, `Ie=e1^e2`, grade-6 pseudoscalar `I=Io^Iinf^Ie` (`I²=−1` ⇒ `Iinv=−I`). Point slots `[1]=x [2]=y [3]=1−x²/4 [4]=1−y²/4 [5]=1+x²/4 [6]=1+y²/4`; origin weight `w=(p3+p4+p5+p6)/4`. Conic `C=p1^p2^p3^p4^eob` is grade 5 = `Dual(IPNS)`; classifier UnDuals it (grade-5→grade-1 linear map `DUAL5`) → `coeffsFromGrade1` `{A=−(c3+c5)/4, B=−(c4+c6)/4, C=0, D=c1, E=c2, F=(c3−c5)+(c4−c6)}` → reused `conicGeometry`. **Scope this cut:** points (finite/round/ideal/infinity) + axis-aligned conics only; grades 2/3/4 (n-poles/pairs) left `mixed`. New dir `src/algebras/aaccga/` (algebra/embed/conic/classify/display/index) mirroring CCGA — `conic.js` geometry (det3, cubic, factorLinePair, lineConicPoints, conicGeometry) copied **verbatim** (dimension-independent); only `coeffsFromGrade1` + the dual map are (4,2)-specific. **Shared engine:** parameterized `ccga/product.js`'s `createEngine` with `posCount` (metric flip `bit>=posCount`; mask table `1<<log2(N)`); CCGA passes 5, AACCGA imports the same engine with 4 — no fork. Named constructors `circle/ellipse/hyperbola/parabola/line/conic` (no `tilted_ellipse` — needs a cross term). Registered in `src/algebras/index.js`. **Veronese-point lock fix:** a round point shifts `einf1`/`einf2` equally, so `isVeronesePoint` checks the *difference* `(E1−½x²)≈(E2−½y²)` (not each), exactly as CCGA does — else `point(0,0,1)` fell to `mixed`. Showcase: 4 points + unit circle via `P1^P2^P3^P4^eob` + `ellipse(2,1)`. **Verification** (`scripts/aaccga_verify.mjs`, new): Part A — sparse engine vs pristine ganja Algebra(4,2) over all 64² basis pairs + unary + random dense → **0 mismatches**; Part B — classify/geometry smoke test (point/round/unit-circle/circle/ellipse/hyperbola/parabola/line) → all PASS. `node scripts/ccga_verify.mjs` re-run → CCGA **unchanged** (engine refactor safe). Full showcase replays through the real parse→evaluate→getRenderPlan pipeline correctly. eslint clean; `npm run build` ✓.

159. **Fix `>>>` sandwich with mvConst (null-basis blade) operands** — "add >>> for example Iinv = e3^e4, Iinv>>>eo1 should return einf1, (e3^e4)*eo1*(e4^e3) = einf1." In AACCGA (and latently CCGA), `R = e3^e4; R >>> eo1` evaluated to **null**. **Root cause — parser routing, not the engine:** `Algebra.sw(R, eo1)=2·einf1` and the mvExpr/`evalMVArith` path were both already correct (`evalMVArith('R >>> eo1',{R})`→`2·einf1`, `extractMVDeps`→`['R']`). But when the **motor is a bare identifier**, `createParseExpression` (`src/graph/parseExpression.js:368`) routes `<ident> >>> <geom>` to the **`motorApply`** node type, whose compute (`nodeTypes.js:116`) resolves operands only as graph items via `depValues` — so an mvConst operand (`eo1/einf1/eob/…`) has no graph value ⇒ `depValues[1]` null ⇒ compute returns null. Proof it was pure routing: `(e3^e4) >>> eo1` (parenthesized motor, rejected by motorApply's `^ID$` test) already fell through to mvExpr and returned `2·einf1`. **Fix** (`src/graph/parseExpression.js`): destructure `mvConsts` from the algebra; build `MVCONST_NAMES` + a longest-first, word-boundary `MVCONST_RE` (`(?<![A-Za-z0-9_])(…)(?![A-Za-z0-9_])` so `einf1` beats prefixes and `I` matches only standalone). In the motorApply branch, **bail to mvExpr** when `motorStr` is an mvConst or `geomStr` references one (`usesMvConst`). General (all algebras with mvConsts; PGA/VGA guarded `?? {}`). **Verified:** AACCGA `R>>>eo1`→`2·einf1`, `R>>>einf1`→`0.5·eo1` (now mvExpr); regressions intact — `M >>> P` and `M >>> point(1,2)` still `motorApply` (deps `["M","P"]`/`["M"]`), CCGA `M >>> P` still `motorApply`, CCGA `R >>> eo1` now correctly mvExpr. `node scripts/aaccga_verify.mjs` + `ccga_verify.mjs` (incl. all saved CCGA graphs) PASS; eslint clean; `npm run build` ✓. Caveat noted (not fixed): naming a graph item like an mvConst (the user's literal `Iinv`) drops it from `extractMVDeps` (pre-existing) — use a non-reserved motor name (e.g. `R`) for live dep tracking.

160. **User-defined items now shadow built-in MV constants** — "Iinv>>>eo1 returns -eo1 it should return the sandwich product (Iinv = e3^e4)." Follow-up to #159: with `Iinv` defined as an item `= e3^e4`, `Iinv >>> eo1` returned `-eo1` (the sandwich of the *built-in* `Iinv` = −I pseudoscalar-inverse with eo1), not the user's `e3^e4`. **Root cause:** `extractMVDeps` excluded every mvConst name (`evalMVArith.js:340`, `… && !(t.val in MV_CONSTS)`), so the user's `Iinv` *item* was never collected as a dependency of the `Iinv >>> eo1` node — its value never entered the eval `env`, and at eval time the name fell through to `CONST_ENV.Iinv` (the constant). `evalMVArith` already layers `env` over `CONST_ENV` (user defs shadow constants *when present in env*), so the fix is to make the dep tracking deliver the item value. **Fix (3 edits, general across algebras):** (1) `evalMVArith.js` — drop the `MV_CONSTS` exclusion in `extractMVDeps` so mvConst-named identifiers ARE collected as deps (a user item of that name is now tracked; a genuine constant simply has no node). (2) `evaluate.js` — relax the per-node guard from `depValues.some(v=>v==null)` to `node.deps.some((d,i)=> nodes[d] && depValues[i]==null)`, so a dep *name with no node* (a constant like `eob`) no longer blocks the node; the `mvExpr` compute resolves it via `CONST_ENV` (and `evalMVArith` only copies *defined* env entries, so undefined deps fall through). (3) `ExpressionPanel.jsx` — exclude mvConst names from the `missingDeps` "create scalar" suggestion so constants don't show as missing. **Verified:** `Iinv = e3^e4; Iinv >>> eo1` → `2·einf1` (deps `["Iinv","eo1"]`, Iinv tracked); `R >>> eo1`/`R >>> einf1` unchanged; AACCGA + CCGA showcases intact (`eob` still resolves as a pure constant). `node scripts/ccga_verify.mjs` — engine identical + **46 saved graphs, 0 integration failures**; `aaccga_verify.mjs` PASS; graph files eslint-clean (ExpressionPanel's 6 errors all pre-existing, none at the edit); `npm run build` ✓.

## 2026-06-26

161. **Complete the AACCGA object taxonomy (grades 2–4)** — "I want to complete the aaccga taxonomy of object … Twopole=E^F, Tripole=E^F^G, Quadpole=E^F^G^H, Conic=…^eob, Circle=…einfb…, Line=…Iinf…, flat point=E^Iinf. complete the list by checking what's been done in CCGA." AACCGA previously classified only grade-1 (points) and grade-5 (conics); grades 2/3/4 fell to `mixed` and drew nothing — blocking the `I4 = C1 & C2` conic-intersection workflow (`saved_graphs/AACGA_conic_inter.json`). **Key discriminator (confirmed with the user):** whether `eob` is a factor, tested `‖Obj ∧ eob‖ ≈ 0`; if so the underlying n-pole is recovered by contracting with `einfb` (eob·einfb = −1 — exactly the user's `I4 | einfb`). **Final taxonomy:** g2 `E^F`→**twopole** (2 pts); g3 `E^F^eob`→**conicPencil** (its 2 base pts), `E^Iinf`→**flatPoint**, `E^F^G`→**tripole** (3 pts); g4 `E^F^G^eob`→**conicIntersection** (3 pts, = `C1&C2`), bare `E^F^G^H`→**conic** *incomplete* (dashed curve — `einfb`/`Iinf` circle/line variants fall out as the conic `subtype` for free); g5 `…^eob`→**conic** *complete* (solid, unchanged). **Crucial simplification vs CCGA:** no 4-point quadpole extractor needed — grade-4 *no-eob* is a conic curve and grade-4 *with-eob* reduces to a tripole. **Files:** `aaccga/algebra.js` (+`FP_B0/BX/BY`, `FP_I0/IX/IY` flat-point blades); `aaccga/embed.js` (+`euclOfSum` reading the 4 origin slots, +`extractFlatPoint`); new `aaccga/extract.js` (`rawNorm`, `extractDipole` on slots 1..6, `extractTripole` via the circumcircle carrier `T∧einfb∧eob` + `wedgeResidualForm` on the **6-coord** point row, sampleCircle/refineChord/findMembershipZeros — all adapted from CCGA's); `aaccga/classify.js` (`hasFactor`, `contractEob = LDot(einfb, val)` — note left-contraction order: `LDot(val, einfb)` is 0 since g3>g1; grade-2/3/4 dispatch + render-plan cases mapping twopole/tripole→`multipole`, flatPoint→`flatPoint`, pencil/intersection→`getRenderPlan(core)`, conic carries `incomplete`); `Canvas.jsx` (conic dashes when `plan.incomplete`); `aaccga/index.js` (+KIND_COLOR for twopole/tripole/flatPoint/conicPencil/conicIntersection). **Verification:** extended `scripts/aaccga_verify.mjs` Part B with the full ladder incl. the real `C1 & C2` (= `AAC.Vee(C1,C2)`) → `conicIntersection`/multipole-3 — all PASS (Part A engine parity still 0 mismatches); `npm run build` ✓.

162. **Flat point from line intersection — classify & normalize as a finite point** — "norm of finite point should be -w = Obj | einf. P = -48.18e1 + 9.989e2 + 20.69eo1 + 20.69eo2, w = 20.69 … P should be considered as a special point: P = (FlatPoint | Io) and rendered as a point like in CCGA. It's constructed as two lines intersection: P = (L1&L2) | (Io^einfb)." The grade-1 result of a line meet is a **flat-point form** `w·eo + x·e1 + y·e2` — eo-weighted but with NO Veronese (einf) part (`p5−p3 = p6−p4 = 0`). It was falling to `mixed`: `extractRoundPoint` reports a spurious r² (here 5.66), `isVeronesePoint` fails, so it neither rendered nor normalized correctly — the norm button divided by `A.Length` (=49.2, the full ‖p‖ mixing in the e1/e2 magnitude) giving `-0.9792e1 + 0.203e2 + 0.4205eo1…` instead of dividing by `w`. **Fix (`aaccga/classify.js`, one helper + one branch):** added `isFlatPointVector(p)` (point-vector with both einf coeffs ≈ 0 vs scale), checked at the top of the grade-1 `isPointVector` branch *before* the round-point split → returns `finitePoint`. This routes both paths correctly with no other changes: `normalizeMVFinit` already divides `finitePoint` by `w = einfWeight = −(p·einf)`, and `getRenderPlan` → `toEuclidean` already reads `(p1/w, p2/w)`. **Verified:** the exact P → `finitePoint`, normalizes to `-2.3287e1 + 0.4828e2 + eo1 + eo2` (÷20.69), renders at `(-2.329, 0.483)`; the real construction `(Io^einfb) | (L1 & L2)` on two grade-5 OPNS lines → grade-1 `finitePoint` at the analytic intersection `(0.2, 1.4)`; no regression on Veronese / round / origin / scaled points.

## 2026-06-29

164. **Export to SVG / PNG / PDF** — "I would like to add a export to svg/pdf/png feature." Decisions: fit-to-content crop, current-theme background, PDF derived directly from the SVG (zero-dep). New `src/export/exportImage.js`: `buildExportSvg(svgEl)` clones the live `<svg>`, inlines every computed presentation style (resolving `var(--…)`/`currentColor` so the file is self-contained), crops the `viewBox` to the geometry's `getBBox()` + 16px padding, and grows the background rect to fill the crop. `downloadSvg` (XMLSerializer→Blob), `downloadPng` (offscreen canvas at 1×/2×/3×, `toBlob`), `printSvg` (hidden iframe + `window.print()` for vector PDF). `Canvas.jsx`: tagged the bg rect `data-export-bg` and wrapped `{layers}` in `<g data-export-content>` (so fit-to-content excludes the full-bleed grid/bg), added a ⬇ overlay button + dropdown menu (SVG · PNG 1×/2×/3× · PDF) and `runExport`. CSS in `App.css` matching the existing ghost overlay buttons. No new deps; `npm run build` ✓, new module eslint-clean.

163. **Merge CCGA + ACGA branches to main (with rename)** — "I would like to merge accga (rename it acga) and ccga branch to main." Renamed the AACCGA algebra to **ACGA** across `feat/aaccga` (dir `src/algebras/aaccga/`→`acga/`, `ID 'aaccga'`→`'acga'`, `LABEL 'AACCGA'`→`'ACGA'`, `aaccgaExp`→`acgaExp`, `scripts/aaccga_verify.mjs`→`acga_verify.mjs`, `saved_graphs/AACGA_conic_inter`+9 local versor/transform graphs→`acga_*` incl. internal `algebra`/`name` fields, and the `AACCGA` comment refs in `ccga/product.js` + registry). Reconciled stale local branches against origin first (origin had advanced both branches + already merged the cga310 Cl(3,1) adapter via PR #8 — `origin/main` is an ancestor of `feat/ccga-algebra`, so no cga310 conflict). Two explicit `--no-ff` merges into main: `feat/ccga-algebra` then `feat/aaccga`; the same-message "add saved graphs" commit on each branch added byte-identical blobs so the add/add auto-resolved cleanly. **Verified on merged main:** `scripts/acga_verify.mjs` + `scripts/ccga_verify.mjs` PASS, `npm run build` ✓, registry lists 6 algebras (pga201, vga200, r010, cga310, ccga, acga), zero stale `aaccga`/`AACGA` tokens. Pushed `main` (afb2019→e3ec5e5) and the renamed `feat/aaccga`.
